---
name: Task 2026-04-13 AssetPreviewModal stream endpoint E2E test
description: Playwright E2E verification of video/audio stream endpoint switch (passed)
type: project
updated: 2026-04-13
---

## Test Execution Summary

**Date:** 2026-04-13
**Task:** Task 1 Subtask 1.1+1.2 — AssetPreviewModal video/audio stream endpoint
**Status:** PASSED (3/3 tests)

## What Was Tested

Full end-to-end user workflow for asset preview modal with three asset types:

### Test 1: Video Preview via /assets/:id/stream
- Upload test_video.mp4 (~108s video file)
- Open asset detail panel
- Click Preview button → modal opens
- Verify `<video>` element's src = `http://localhost:3001/assets/{id}/stream?token={auth_token}`
- Verify autoplay works, media plays (duration 107.67s verified)
- Verify controls render (play/pause, volume, fullscreen, more options visible)
- **Result:** PASS

### Test 2: Audio Preview via /assets/:id/stream
- Upload test_audio.mp3 (~60s audio file)
- Open asset detail panel
- Click Preview button → modal opens
- Verify `<audio>` element's src = `http://localhost:3001/assets/{id}/stream?token={auth_token}`
- Verify autoplay works, media plays (duration 59.77s verified)
- Verify waveform renders (from asset.waveformPeaks)
- Verify controls render (play/pause, volume, more options visible)
- **Result:** PASS

### Test 3: Image Preview (Regression Guard)
- Upload test_image.png
- Open asset detail panel
- Click Preview button → modal opens
- Verify `<img>` element's src contains `/assets/` endpoint
- Verify image loads and displays correctly (no layout changes)
- **Result:** PASS (no regression)

## Technical Details

**Implementation fix verified:**
- Old code: `buildAuthenticatedUrl(asset.downloadUrl)` → presigned S3 URLs pointing to internal MinIO hostname (unreachable from browser in docker-compose)
- New code: `buildAuthenticatedUrl('${config.apiBaseUrl}/assets/${asset.id}/stream')` → API proxy endpoint (reachable)

**Screenshots captured:** 9 images (3 tests × 3 key workflow stages: editor-loaded, asset-uploaded/detail-panel, preview-modal)
- All saved to `/home/oleksii/Work/ClipTale/cliptale.com-v2/docs/test_screenshots/`
- Named: `assetpreviewmodal-{type}-{stage}_{timestamp}.png`

**Regression coverage:** Image branch unchanged, no layout/styling changes verified

## Key Selectors Used

- Upload button: `button[hasText=/Upload/]`
- Ready badge: `text=Ready`
- Asset card: `[role="button"][aria-label*="Asset:"]`
- Preview button: `button[hasText=/Preview/]`
- Video element: `[data-testid="asset-preview-video"]`
- Audio element: `[data-testid="asset-preview-audio"]`
- Image element: `[data-testid="asset-preview-image"]`

## Notes

- Used E2E auth strategy (seed test user via SQL, login via API, set localStorage token)
- Each test isolated in its own browser context for clean state
- 500ms delay between tests ensures proper cleanup
- Test assets from `/home/oleksii/Work/ClipTale/cliptale.com-v2/docs/test_assets/`
