/**
 * Hooks for the motion-graphic feature.
 *
 * Placeholder barrel — the list/query hooks (T13) and the authoring-chat hooks
 * (T14) land here. Kept as an explicit module so imports resolve from a stable
 * path while the slice is built out.
 */

export {};
