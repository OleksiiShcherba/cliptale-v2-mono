/**
 * Browser runtime for the motion-graphic feature.
 *
 * Placeholder barrel — the SSE stream client (generate/refine), the live-preview
 * transpile + AST determinism scan (AC-09), and the verdict reporter land here
 * (T14/T15). Kept as an explicit module so imports resolve from a stable path
 * while the slice is built out.
 */

export {};
