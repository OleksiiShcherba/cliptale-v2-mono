# Memory Index

- [Split test files must share fixtures via .fixtures.ts — not duplicate them](project_split_test_fixtures.md) — render.job split produced verbatim fixture duplication; render.job.fixtures.ts was never created
