---
feature: scene-generation-reference-gate
date: "2026-06-10"
reviewer: "Independent clean-context review (sdd:reviewer ×3) — Tech Lead gate"
verdict: CHANGES REQUESTED
---

# Review — scene-generation-reference-gate (2026-06-10)

## Scope

- Diff: `git diff 0063cba..HEAD` on `feat/scene-generation-reference-gate`.
- **70 files changed, 6560 insertions(+), 4274 deletions(-)**.
- Surfaces (sad `target_surfaces`): `backend-service` (apps/api), `web-frontend` (apps/web-editor), `worker` (apps/media-worker) + `packages/api-contracts` + docs.
- SDD-AC trailers across commits cover the full §5 set: AC-01, AC-02, AC-03, AC-03b, AC-04, AC-04b, AC-05, AC-06, AC-06b, AC-07, AC-08, AC-09.

Three clean-context reviewers ran in parallel: stage-1 end-to-end AC/US trace; stage-2 quality on api+worker+contracts; stage-2 quality on web-frontend.

## Stage 1 — AC / user-story trace

Every §4 user story (US-01..US-07) has ≥1 AC and a §6 flow. Every §5 AC reaches production code + an asserting test on the **backend and worker** surfaces. High-risk traces verified by reading code:

- **AC-02 / AC-07** — readiness = persisted output-existence (`flow_id IS NOT NULL AND EXISTS(flow_files … deleted_at IS NULL)`), not `window_status`, not a star: `apps/api/src/repositories/storyboardReference.repository.ts:441-448, 481-488`; AC-07 still-generating asserted `storyboard-illustration-endpoints.test.ts:927`.
- **AC-04 vs AC-04b** — zero-block draft passes (`storyboardIllustration.service.ts:63`), referenced draft runs unlinked-scene check (`:65-73`); asserted `:993, :1008`.
- **AC-05** — reference boundary, worker reads only linked blocks: `referenceSelection.ts:59-61`; invariant asserted `storyboardOpenAIImage.job.integration.test.ts:489`.
- **AC-06 / AC-06b** — one output/linked block, primary-star-if-usable else latest completed (created_at DESC, file_id DESC), deleted-star fallback: `referenceSelection.ts:69-84`; integration `:533`.
- **AC-08** — principal fully retired (routes 404, types stripped, ignore-on-read): asserted `:540, :636`.
- **AC-09** — owner-scoped resolve precedes gate eval: `storyboardIllustration.service.ts:112-113, 145-146`; asserted `:1077`.

**One AC dropped out of the chain on a declared surface → stage-1 finding F1 below.**

## Findings & verdicts

| # | Severity | File:line | Finding | Verdict |
|---|---|---|---|---|
| F1 | **BLOCKER (stage-1, AC-03b)** | `apps/web-editor/src/features/storyboard/hooks/useStoryboardIllustrations.ts:232-237` | `retryBlock` (per-scene regenerate) catch never inspects `err.code/err.details` nor calls `setGateError` (unlike `start()` at `:204-218`), so a scene-scoped `references.reference_gate_failed` 422 is swallowed — the named scene-linked blocking blocks AC-03b mandates are never rendered. No test catches it: the component test (`ReferenceGateMessage.test.tsx:208`) is a prop-level tautology; no hook test drives `startStoryboardBlockIllustration` through a 422. AC-03b unsatisfied on the `web-frontend` surface. | **Fix now** |
| F2 | MAJOR (quality) | `apps/media-worker/src/jobs/storyboardOpenAIImage.job.ts:311-315, 332-336` + `workerRepositories.ts:172-199` | Dead `style_reference` branch still writes the deprecated `storyboard_illustration_references` table via `storyboardReferenceRepo`. No producer enqueues `style_reference` after this feature (createReferenceJob/ensureReadyReference deleted) — unreachable dead code that contradicts ADR-0004 retire + KPI principal_image_generations → 0. | **Fix now** |
| F3 | MINOR | `packages/api-contracts/src/openapi.ts:3015-3049` | Orphaned principal request-body schemas (`EditPrincipalImageBody`, `ReplacePrincipalImageBody`, `SetPrincipalImageReferencesBody`) — paths removed, schemas left; drifts the generated contract (SAD §8 lockstep). | **Fix now** |
| F4 | MINOR | `apps/api/src/services/storyboardIllustration.validation.ts:61, 73` | Orphaned exports `buildReferencePrompt` / `resolveDraftImageReferenceFileIds` — sole scene-path consumer (createReferenceJob) deleted. | **Fix now** |
| F5 | MINOR | `apps/web-editor/src/features/storyboard/components/StoryboardPlanControls.tsx:177-180` | Stale user-visible copy "Scene images start automatically after the principal image is approved." — AC-08 surface leak (retired step referenced in idle state). | **Fix now** |
| F6 | MINOR | `apps/web-editor/src/features/storyboard/hooks/useStoryboardIllustrations.ts:196` (and `:163`) | Vestigial `phase: 'reference'` set in the post-AC-08 single-track flow; "Creating visual style reference" phase still renderable (`StoryboardPlanControls.tsx:165-169`). | **Fix now** |
| F7 | MINOR | `apps/api/src/services/storyboardIllustration.service.ts:53-54, 89-90` | Backend gate `error` string omits the "retry / remove" guidance AC-02 / openapi example mandate (UI delivers it via `ReferenceGateMessage.tsx:46-48`; contract `code`+`details.blocks` correct — no AC gap, weaker human string only). | **Fix now** |

All seven findings resolved **Fix now** by the owner (2026-06-10). No deferred or dismissed findings.

## Verdict

**CHANGES REQUESTED** — one open stage-1 AC finding (F1, AC-03b unsatisfied on the web-frontend surface) plus a MAJOR dead-code cleanup (F2) and five MINOR cleanups (F3–F7), all accepted for fix.

Next: `/sdd:implement scene-generation-reference-gate` for the fixes (no `/clear` — stay in context), then re-review the changed surface (web-frontend per-scene gate path + worker job + contract).
