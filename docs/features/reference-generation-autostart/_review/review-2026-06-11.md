# Review record — reference-generation-autostart

- **Date:** 2026-06-11
- **Reviewer:** independent clean-context `sdd:reviewer` (did not author the code)
- **Branch:** `feat/reference-generation-autostart`
- **Base:** `af8b58e4ff415bc5d3036985c0d4d77af93c945c` (merge-base with `master`)
- **Verdict:** **PASS**

## Scope

`git diff af8b58e..HEAD` — 19 files, +1015 / −121.

Production surfaces (per `sad.md target_surfaces: [web-frontend, backend-service]`):
- `apps/api/src/services/storyboardReference.extraction.service.ts` — idempotent start (ADR-0001)
- `apps/api/src/controllers/storyboardReference.controller.ts` — widened status union at 202
- `apps/web-editor/src/features/storyboard/hooks/useCastAutostart.ts` — NEW (mount existence-check + conditional start + in-flight guard + poll)
- `apps/web-editor/src/features/storyboard/components/CastConfirmModal.tsx` + `.styles.ts` — backdrop+dialog wrapper, distinct states
- `apps/web-editor/src/features/storyboard/components/StoryboardPage.tsx` — wiring
- `apps/web-editor/src/features/storyboard/api.ts` — widened client return type
- test files (component · unit · integration-against-real-MySQL)

## Stage 1 — AC / user-story end-to-end trace

Every §4 user story (US-01..US-06) and every §5 AC (AC-01..AC-07) traced spec → §6 flow → contract → tasks.json → code → asserting test. **No AC dropped out of the chain.**

| AC | Task | Production code | Asserting test | Verdict |
|---|---|---|---|---|
| AC-01 auto-start happy path | T1/T5/T6 | `useCastAutostart.ts:64-85`; `StoryboardPage.tsx:110`; `extraction.service.ts:85-95` | `useCastAutostart.test.ts:79-90`; `StoryboardPage.test.tsx:310-319` | SATISFIED |
| AC-02 real dialog, 0 stray buttons | T3 | `CastConfirmModal.tsx:146-194`; `.styles.ts:12-33` | `CastConfirmModal.test.tsx:493-513` + regression `617-638` | SATISFIED |
| AC-03 progress visible, no confirm | T4 | `CastConfirmModal.tsx:238-247` | `CastConfirmModal.test.tsx:546-557` | SATISFIED |
| AC-04 consent before charge | T4/T7 | `CastConfirmModal.tsx:302-333`; `StoryboardPage.tsx:135-142` | `CastConfirmModal.test.tsx:592-609`+`648-657`; `StoryboardPage.test.tsx:368-402` | SATISFIED |
| AC-05 one extraction per draft (ADR-0001) | T1/T2/T5 | `extraction.service.ts:75-83`; `useCastAutostart.ts:28,67-68` | **QG-3** `extraction.service.integration.test.ts:114-139` (2nd call → same id, COUNT=1); `useCastAutostart.test.ts:110-130`; `StoryboardPage.test.tsx:321-331` | SATISFIED |
| AC-06 completed-empty | T4 | `CastConfirmModal.tsx:275-284` | `CastConfirmModal.test.tsx:565-585` | SATISFIED |
| AC-07 manual recovery after failed auto-start | T1/T6 | `extraction.service.ts:81-95` (failed=not-existing → fresh); `StoryboardPage.tsx:117-133` | `extraction.service.integration.test.ts:141-150` (failed→fresh queued, COUNT=2); `StoryboardPage.test.tsx:333-348` | SATISFIED |

Idempotent-start semantics confirmed: `failed` = not-existing (new start allowed), queued/running/completed returned as-is; QG-3 asserted against real MySQL with the row-count invariant. UI ACs all carry component + wiring coverage through the real component (not backend-only).

## Stage 2 — quality findings & resolution

| # | Finding | Severity | Resolution |
|---|---|---|---|
| 1 | Stale JSDoc `api.ts:622` said `/references/extraction/latest`; code + contract use `/references/extraction` | nit | **Fixed now** — comment corrected. |
| 2 | Test-plan §"Cleanup boundary" said "truncate/rollback per test"; implementation isolates per-unique-`seedDraft()` + `afterAll` cleanup (actually correct, wording diverged) | nit | **Fixed now** — `spec.md` test-plan wording aligned to the implemented per-draft isolation. |
| 3 | `act()` warnings from `await new Promise(setTimeout)` negative-waits in `StoryboardPage.test.tsx` (tests passed, assertions real) | nit | **Fixed now** — negative-waits wrapped in `await act(async () => …)`; re-run shows 0 `act()` warnings, 21/21 green. |

No stage-1 AC gaps. No quality blockers. The three e2e-through-UI journeys (AC-01/04/07) are deferred to the scheduled/pre-release CI track per the test-plan's explicit split — every UI AC retains component + wiring coverage, so the surface is not backend-only.

Items checked & cleared (not findings): React 18 StrictMode double-effect covered by module-level `attemptedAutostart` guard + server idempotency; in-flight guard genuinely asserted; owner-scoped check inherited (no new authz boundary); no charge on the free path (`confirmCast` fires only on explicit click).

## Gate result

**PASS** — 0 stage-1 AC gaps; 3 nits, all resolved (fixed now). 21/21 StoryboardPage tests green after the fix; QG-3 idempotency asserted against real MySQL.

## Next

`/clear`, then `/sdd:ship reference-generation-autostart`.
