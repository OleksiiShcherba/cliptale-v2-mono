# Re-review (r5) — storyboard-generation-pipeline (verify r4 fixes)

- **Date:** 2026-06-15
- **Reviewer:** independent clean-context (sdd:reviewer — focused re-review of the r4 changed surface), verified in-context.
- **Scope:** the two r4 fix commits — `f51844e` (F1) + `c978fec` (F2). Touched: `StoryboardPlanControls.tsx` (+test), `api.ts`, `pipeline.e2e.test.tsx`, `storyboardPipeline.controller.ts`, `contracts/openapi.yaml`.
- **Context:** r4 (CHANGES REQUESTED) raised two minors, both "Fix now": F1 (AC-12 — no co-located Retry on a failed scene_image phase) and F2 (contract/AC-03 — dead `references` field in `CastConfirmation`). The whole-feature §4/§5 AC trace was already fully clean in r4 (all 15 AC TRACED across be/wk/web); r5 verifies only the two fixes.

## Verdict: **PASS**

Both r4 findings RESOLVED with genuine tests; three-way contract consistency intact; no new defects. No stage-1 (AC) finding remains open. The full §4 user-story / §5 AC chain is satisfied end-to-end (r3 + r4 + r5 cumulatively).

---

## Findings re-verified

**F1 (AC-12) — RESOLVED.**
- `StoryboardPlanControls.tsx` — `StoryboardIllustrationControls` renders a Retry button (`data-testid="storyboard-illustration-retry-button"`) in the `failed` branch, gated `showRetry = status === 'failed' && !hasStructuredGateError`, wired to `onRegenerate`.
- Wiring traced end-to-end: `StoryboardPageWorkspace.tsx:208` `onRegenerate={handleIllustrationRegenerate}` → `:162-164` `illustrationGeneration.start()` → `useStoryboardGenerationFlow.ts:148/126-128` `triggerPhase(draftId, 'scene_image')`. Correct phase.
- Suppression under a structured gate error is correct: a prerequisite block (AC-08/AC-15) is not a phase failure and is driven by its dedicated guard message + corner controls — offering a phase-retry there would be wrong.
- Tests (3 new, non-tautological): Retry present + calls onRegenerate on a real `failed`; absent under `hasStructuredGateError`; absent across `idle/queued/running/completed`. 15/15 file green.

**F2 (contract / AC-03) — RESOLVED.** Three-way consistent:
- `contracts/openapi.yaml` `CastConfirmation`: `references` removed → `properties: {}`, `additionalProperties: false`, honest confirm-as-shown description. `ProposedReference` still referenced by `CastProposal` (not orphaned).
- `storyboardPipeline.controller.ts` `confirmCastBodySchema`: `references` removed; `cost_estimate` KEPT (live defensive re-validation input — `confirm.service.ts:203` + `controller.test.ts:302`). 11/11 controller tests green.
- `api.ts` `confirmPipelineCast`: body param dropped, posts `{}`. Sole caller `StoryboardPage.tsx:564` passes no body; e2e mock typed `[string]` + asserts `toHaveBeenCalledWith(DRAFT_ID)`. 13/13 e2e green.

## New defects
None. Retry reuses the existing `s.button` style (already used by the scene-phase retry); no new style/type issue; no caller still passes a confirm body.

## Gate (from implement r4)
- Web-editor storyboard suite: 76 files / 752 tests green. API controller: 11/11 green. Typecheck clean on all changed files.

## Resolutions
| Finding | Severity | Resolution |
|---|---|---|
| r4-F1 scene_image co-located Retry | Minor (AC-12) | **RESOLVED** |
| r4-F2 dead `references` confirm field | Minor (contract/AC-03) | **RESOLVED** |

No open finding. Feature is review-clean end-to-end → ship.
