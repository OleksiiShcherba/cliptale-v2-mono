# Review r12 — storyboard-generation-pipeline

- **Date:** 2026-06-16
- **Reviewer:** sdd:reviewer (clean-context, independent)
- **Scope:** 4 commits since r11 (`705a766`…`63e53d2`) — base-flow-per-reference, default-position persistence, StepCorners toolbar relocation, E2E mock updates

## Verdict: **PASS — REVIEW_CLEAN**

All ACs (AC-01…AC-15) and all user stories (US-01…US-08) remain fully traced spec → sequences → data-model → api → tasks → code → test. No AC or US dropped out. Both stage-1 and stage-2 are clean.

## Diff stat

```
5 changed source files + e2e + backend unit tests since r11
```

## Stage 1 — AC completeness: STAGE_1_CLEAN

| Commit | Surface | AC(s) touched | Evidence |
|--------|---------|---------------|---------|
| `705a766` | `storyboardPipeline.confirm.service.ts` — base flow created per reference block | AC-03 ("View flow" reachable) | `confirm.service.test.ts:440+` — flow_id non-null, generation_flows row exists, canvas contains a generation block |
| `a32dade` | `useStoryboardCanvas.ts` — PATCH default positions on canvas load | AC-09 (reference block placement) | Flow 9 e2e asserts storedX=340, storedY=442; PATCH fires exactly once |
| `63e53d2` | `StepCorners.tsx` — canvasToolbarButton style, bottom-left placement | AC-08/AC-15 guard messages | `StepCorners.test.tsx` — out-of-order + scenes-required alerts still surfaced via `role="alert"` |
| `803063a` | E2E mock updates | All pipeline flows | 19/19 pipeline + 10/10 reference flows/gate pass |

## Stage 2 — Code quality: STAGE_2_CLEAN

| File | Finding |
|------|---------|
| `StepCorners.tsx:88-92` | GateError messages still surfaced verbatim in `role="alert"` — AC-08/AC-15 contract intact after restyle |
| `StoryboardCanvas.tsx:164-165` | StepCorners guarded behind truthy `draftId`; `pipelineState ?? null` normalized — no NPE path |
| `zoomToolbarStyles.ts:14-25` | Absolute positioning removed and re-homed to `BOTTOM_LEFT_TOOLBAR_STYLE` in the canvas wrapper — no orphaned styles |
| `useStoryboardCanvas.ts:457-466` | PATCH-on-load is fire-and-forget with mount guard; non-zero positions skip the CAS (guarded at line 141) — no write loop on reload |
| `confirm.service.ts:288-303` | `claimRun` CAS gate still prevents duplicate base-flow creation — AC-14 idempotency preserved |

## Notes (non-blocking, carried from earlier rounds)

- **`b39fa2d "multiple fixes"`** landed on the branch (canvas layout: `useStoryboardCanvas.ts`, `musicBlockLayout.ts`, `buildStoryboardLayout.ts`, plus `e2e/storyboard-canvas-layout.spec.ts`). It is canvas-layout plumbing only — no AC-logic change — and was intended for this branch.
- **Sequential `pool.execute` in confirmCast** (`confirm.service.ts:317-385`): the new `generation_flows` INSERT slightly widens the non-atomic window. Pre-existing pattern; CAS claim prevents duplicate sets so AC-14 holds. Flagged for awareness only.

## Test summary

| Suite | Tests | Result |
|-------|-------|--------|
| `e2e/storyboard-generation-pipeline.spec.ts` | 19 | ✅ pass |
| `e2e/storyboard-reference-flows.spec.ts` | 7 | ✅ pass |
| `e2e/storyboard-reference-gate.spec.ts` | 3 | ✅ pass |
| `apps/api/src/services/storyboardPipeline.confirm.service.test.ts` | 10 | ✅ pass |
| **Total** | **39** | **✅ 39/39** |

## Next

`/clear`, then `/sdd:ship storyboard-generation-pipeline`
