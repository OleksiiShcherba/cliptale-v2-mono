# Re-review (r2) — storyboard-generation-pipeline

- **Date:** 2026-06-15
- **Reviewer:** independent clean-context (sdd:reviewer) over the fix diff
- **Scope:** `git diff 84ec3f4..HEAD` — fix commits `4c7c52a` (worker) + `b67374b` (api).
- **Context:** verification pass for the 10 findings of [review-2026-06-15.md](./review-2026-06-15.md) (r1 = CHANGES REQUESTED, all resolved Fix now).

## Verdict: **PASS / CLEAN** (backend + worker checkpoint, T1–T14)

All 10 r1 findings genuinely resolved with non-tautological, behavioral tests against real MySQL; no regressions introduced.

| Finding | Verdict |
|---|---|
| B1 (AC-02 cast-extract enqueue) | RESOLVED — `enqueueCastExtraction.ts` (row+enqueue, idempotent), wired Queue producer in `index.ts`, called only on `advanced===true`; tests cover enqueue+idempotency+gating+best-effort |
| B2 (AC-12 heartbeat refresh) | RESOLVED — `refreshActiveRunHeartbeat` called per reference block (`ai-generate.job.ts`) + per scene (`storyboardOpenAIImage.job.ts`); test re-stamps stale + asserts non-active phase not refreshed |
| G2 (AC-04 scene-image estimate) | RESOLVED — `sceneCount × perUnit` persisted to cost_estimate + payload in `onReferenceImagesAllTerminal`; test |
| G3 (AC-02/03 cast payload + ref estimate) | RESOLVED — `onCastProposalReady` persists `payload.cast_proposal` (shape matches openapi) + `castSize × perUnit`; same pricing source as confirm; test |
| G4 (AC-07 skip gate) | RESOLVED — `awaiting_review` precondition → `pipeline.not_awaiting_review` (422); idle/running reject tests |
| G5 (AC-03 no-body confirm) | RESOLVED — re-validate only when client estimate supplied; tampered still rejects; both tested |
| MIN-1 (env→config) | RESOLVED — `config.storyboardPipeline.stuckPhaseBoundMinutes`; no process.env in resume.service |
| MIN-2 (reaper pool.query) | RESOLVED |
| MIN-3 (releaseStuckPhase + stale test) | RESOLVED — exported + direct affectedRows===0 test |
| MIN-4 (concurrent double-confirm) | RESOLVED — Promise.all race test, one block set |

## Regression / consistency checks — all clear

- G3 worker estimate (`castSize × queryPerUnitCost('openai/gpt-image-2')`) matches api `computeReferenceImageEstimate` exactly (same table, formula, count via zod-validated proposal) → an echoed confirm-as-shown does NOT 422.
- cast-extract's `aggregateEstimateCredits` (different price table) is stored only on the cast-extraction job row, never on `storyboard_pipeline.cost_estimate` — no cross-contamination.
- reference_image estimate-vs-actual delta reads `state.cost_estimate` BEFORE `casAdvance` overwrites it with the scene-image estimate → telemetry intact.
- Hook order: cast-extract marks completed (proposal_json) before `onCastProposalReady` reads it → no empty-payload race.
- Dynamic column names remain whitelist-mapped (injection-safe).

## Gate

- Worker: 38/38 files, 305/305 tests (real MySQL); tsc 0 errors.
- API: lifecycle + confirm suites green; full api 2074 passed / 4 skipped / 2 todo, with **1 pre-existing env failure** (`migration-014.test.ts` beforeAll `DROP TABLE ai_generation_jobs` blocked by the migration-053 FK `fk_storyboard_scene_illustration_ai_job`) — confirmed to fail identically on the base commit with all changes stashed; unrelated to this work. tsc 0 errors.

## Standing caveat (unchanged from r1)

The **UI surface (T15–T19), e2e-through-UI (T20), and deploy cut-over (T21) remain unimplemented**. The backend+worker checkpoint is internally complete and clean, but the whole feature is NOT shippable until the UI block lands with its e2e/authz regression. Next implement block: T15 → T16–T19 → T20 → T21.
