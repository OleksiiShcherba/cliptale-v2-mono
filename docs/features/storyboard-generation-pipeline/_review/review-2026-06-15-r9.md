# Review (r9) — storyboard-generation-pipeline (Playwright E2E test suite)

- **Date:** 2026-06-15
- **Reviewer:** Playwright E2E suite driven against the real rendered UI (browser) with mocked
  pipeline API responses (`page.route()`) and mock realtime events (`installMockRealtime` +
  `emitMockRealtimeEvent`). All 16 tests passed in 12.2 s with zero real AI-provider calls.
  Media-worker confirmed running at `AI_GENERATION_STATE=test` (image/video jobs return local test
  assets, no provider spend).
- **Scope:** `e2e/storyboard-generation-pipeline.spec.ts` — 7 describe groups, 16 tests.

## Verdict: **PASS**

All flows pass. Two test bugs were found and fixed during this pass (not production bugs):
- AC-08 test originally tried to `click()` a disabled button → fixed to assert `.toBeDisabled()`.
- AC-12 test originally simulated `scene` phase failing → `PipelineFailureBanner` correctly only
  covers `reference_data` / `reference_image` (scene has its own status-block controls); test
  corrected to use `reference_data: failed` and `reference_image: failed`.

---

## Coverage summary

| Flow | Tests | ACs covered | Result |
|---|---|---|---|
| F1 — Happy path | 3 | AC-01, AC-02, AC-03, AC-04 | ✅ PASS |
| F2 — Skip cast (cascade) | 2 | AC-07, AC-11 | ✅ PASS |
| F3 — Cancel + corner re-trigger | 2 | AC-06, AC-07 | ✅ PASS |
| F4 — Resume | 3 | AC-05 | ✅ PASS |
| F5 — Phase-order guard | 2 | AC-08, AC-15 | ✅ PASS |
| F6 — Realtime version-guard | 2 | AC-05 (observer convergence) | ✅ PASS |
| F7 — Failure banner | 2 | AC-12 (reference_data + reference_image) | ✅ PASS |

**Total: 16/16 passed.**

---

## Notes

- F4 (scene blocks lost after cancel → re-trigger → reload) remains deferred to spec §8 per r8.
  This is an edge race specific to the live stack, not covered by the mocked E2E suite.
- The storyboard plan and cast extraction phases still call the real OpenAI API even in
  `AI_GENERATION_STATE=test` mode (only image/video jobs are stubbed). The mocked Playwright suite
  circumvents this by stubbing the pipeline API entirely — no real pipeline processing runs.
