# Re-review (round 2) — storyboard-reference-flows — 2026-06-07

**Verdict: CHANGES REQUESTED**

Re-review of the F1–F14 fixes from the first review (`review-2026-06-07.md`, CHANGES REQUESTED). Independent, clean-context: 3 `sdd:reviewer` passes, one per finding cluster (F1–F4 / F5–F10 / F11–F14), each verifying genuine closure on the **production** path (not test-only / mocked) and flagging new regressions.

## Scope

- Fix diff: `7d18403..HEAD` (the fixes landed after the first review), 33 files, +1314 / −715.
- Includes deletion of dead `generation-flow.stars.service.ts` (−170) + its test (−450); new `apps/media-worker/src/jobs/workerRepositories.ts`; migration `056_add_truncated_to_cast_extraction_jobs.sql`; new `storyboardReference.stars.service.test.ts` (+360); new co-located `*.styles.ts`.
- Full feature diff: `9ccfb31..HEAD`.

## Test gate (feature surface)

- `apps/media-worker`: **249/249 pass** (31 files).
- `apps/web-editor`: **3146/3146 pass** (297 files).
- `apps/api`: **1912 pass**, 1 failed suite — `src/__tests__/integration/migration-014.test.ts` (`ER_FK_CANNOT_DROP_PARENT` on `ai_generation_jobs` teardown). Pre-existing test-isolation flake, **not touched by any feature commit**, unrelated to this surface (056 only adds a column to `cast_extraction_jobs`). Not attributable to the feature.

## First-review findings — closure status

13 of 14 GENUINELY CLOSED on the production path with non-mocked tests:

| # | Status | Evidence (production path) |
|---|--------|---------------------------|
| F1 (AC-06/07/10) | CLOSED | `listBlocks`→`enrichBlocks` populates stars/previewFileId(primary)/sceneBlockIds; real-MySQL test `blocks.service.test.ts:693-749`. `blocks.service.ts:212-292`; `controller.ts:282-285,570-572`; `openapi.yaml:932-975`. |
| F2 (AC-03/04) | CLOSED | confirmCast/retryBlock claim dispatched blocks to `running`; completion hook advances + writes `failed`+reason. `confirm.service.ts:352-358`; `blocks.service.ts:526-531`; `ai-generate.referenceWindow.ts:82-102`. DB-asserting tests, not mocked. |
| F3 (AC-09/08b) | CLOSED | `buildStoryboardOpenAIImageJobDeps` constructs+passes real `sceneReferenceSelectionRepo`; prod entrypoint uses it. `workerRepositories.ts:253-338`; `index.ts:179-183`; deps test `workerRepositories.deps.test.ts:17-29`. |
| F4 (AC-02) | **PARTIAL → R1** | API/UI/storage/contract side closed & consistently named `truncated` (migration 056; `repository.ts:197-205`; `controller.ts:153`; `CastConfirmModal.tsx:225`; `openapi.yaml:862-867`). BUT the worker write path is unreachable in prod — see R1. |
| F5 (AC-10) | CLOSED | `saveSceneLinks` throws `SceneNotInDraftError` (422 `references.scene_not_in_draft`) for out-of-draft scenes. `blocks.service.ts:595-608`; `errors.ts:127-136`; test `blocks.service.test.ts:757-833`. |
| F6 (AC-14b) | CLOSED | `findDraftBadgesByFlowIds` filters `gd.deleted_at IS NULL`; test exercises real soft-delete service path. `generation-flow.repository.ts:179-186`; `generation-flow.service.srf-t12.integration.test.ts:307-338`. |
| F7 (AC-06/§6.1) SEVERE | CLOSED | Wired stars service calls `assertFileInFlow` before any star write; dead duplicate deleted. `storyboardReference.stars.service.ts:107-122,165`. Cross-tenant file rejected on real path. |
| F8 (AC-06/07) | CLOSED | New real integration suite on the WIRED service: cross-tenant refusal + AC-07 primary-fallback asserted. `storyboardReference.stars.service.test.ts:241-296`. |
| F9 (AC-10b) | CLOSED | Real `it` asserts reorder leaves scene links unchanged. `blocks.service.test.ts:839-859`. |
| F10 (AC-11) | CLOSED | `createBlock` calls `checkFlowRateLimit` before any write; test asserts `RateLimitedError` + zero blocks when capped. `blocks.service.ts:347-356`. |
| F11 (AC-12/13) | CLOSED | Ownership/existence-hiding gate runs BEFORE the linked-block 409 probe; non-owner gets 404. `generation-flow.service.ts:178-196`; test `...srf-t12.integration.test.ts:342-369`. |
| F12 (AC-03/§6.1) | CLOSED | confirmCast validates every `imageFileId` is owner-owned + not soft-deleted in-tx before embed; rejects foreign with rollback. `confirm.service.ts:202-219`; test `confirm.service.test.ts:626-678`. |
| F13 (AC-01/03/06/08/09) | CLOSED | e2e `beforeAll`/`beforeEach` hard-assert `referenceBlocksWired === true`; zero `test.skip(!...wired)` remain. `e2e/...spec.ts:589-594,607,687`. |
| F14 (quality) | CLOSED | Inline hex extracted to co-located `*.styles.ts` using shared tokens (5 new in `nodeStyles.ts:22-25`); zero raw hex in TSX. |

## Findings to fix (resolved with owner 2026-06-07 — all **Fix now**)

### Stage 1 — blocking

| # | Finding | AC | Citation | Resolution |
|---|---------|----|----------|------------|
| R1 | F4 closed on API/UI/storage/contract, but the overflow notice never fires in prod because the **cast-extract worker is unregistered**: `storyboardPlanWorker` calls `processStoryboardPlanJob` unconditionally for every job on `QUEUE_STORYBOARD_PLAN` and never branches on `job.name`; the feature enqueues `cast-extract`-named jobs onto that queue, so `processCastExtractJob` is dead in prod. Cast extraction (US-01/AC-01) AND the overflow notice (AC-02) do not run end-to-end. | AC-01, AC-02 | `apps/media-worker/src/index.ts:155-159`; `apps/api/src/queues/jobs/enqueue-cast-extract.ts:14`; `apps/media-worker/src/jobs/cast-extract.job.ts:265`; `apps/media-worker/src/jobs/storyboardPlan.job.ts` (no job.name branch) | **Fix now** — branch `storyboardPlanWorker` on `job.name` → `processCastExtractJob` with a real repo, + an integration test that asserts the end-to-end `truncated` write through the production worker routing. |

### Stage 2 — quality

| # | Finding | AC | Citation | Resolution |
|---|---------|----|----------|------------|
| R2 | Contract drift introduced by F2: `RetryAccepted.windowStatus` enum is `[pending]` but `retryBlock` now returns `running`; controller echoes it. No consumer asserts on it today. | AC-04 | `contracts/openapi.yaml:1010` vs `blocks.service.ts:529`; `controller.ts:416` | **Fix now** — align the enum to include/return `running`. |
| R3 | createBlock 429 emits code `flow.rate_limited`; contract documents `references.rate_limited` for `POST .../blocks` (contract self-flags as reconcile-at-implement). Status 429 + `Retry-After` correct. | AC-11 | `apps/api/src/lib/errors.ts:163` vs `contracts/openapi.yaml:347` | **Fix now** — reconcile code↔contract (pick one, record decision). |
| R4 | Stale docstring: e2e module comment says "Tests skip with a loud reason" — behaviour is now hard-assert. | n/a | `e2e/storyboard-reference-flows.spec.ts:27-31` | **Fix now** — update the comment. |

## Gate result

**CHANGES REQUESTED** — 1 blocking stage-1 (R1: cast-extract worker unwired → AC-01/AC-02 not end-to-end in prod) + 3 stage-2 quality items (R2/R3/R4). All routed back to `implement` as fix-now follow-ups. 13 of the original 14 findings confirmed genuinely closed. Re-review the changed surface after R1–R4 land.
