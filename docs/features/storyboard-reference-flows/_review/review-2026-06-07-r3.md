# Re-review (round 3) — storyboard-reference-flows — 2026-06-07

**Verdict: PASS**

Re-review of the R1–R4 fixes from round 2 (`review-2026-06-07-r2.md`, CHANGES REQUESTED). Independent, clean-context `sdd:reviewer` pass over the fix diff, verifying genuine closure on the **production** path (not test-only / mocked) and scanning for regressions the fixes introduced. Rounds 1–2 already confirmed 13 of the original 14 findings closed; this round closes the last open one (R1) plus the three quality items (R2–R4).

## Scope

- Fix diff: `6c51820..HEAD` (the R1–R4 fixes), 8 files, +361 / −21.
- Touched: `apps/media-worker/src/index.ts` (queue routing), new `storyboardPlanQueue.processor.ts` + `.test.ts`, `workerRepositories.ts` (+real `castExtractJobRepo`) + `.test.ts`, `e2e/storyboard-reference-flows.spec.ts` (docstring), `contracts/openapi.yaml` + `contracts/api-sync-report.md`.
- Full feature diff: `9ccfb31..HEAD`.

## Round-2 findings — closure status

All four GENUINELY CLOSED on the production path with non-mocked-repo coverage:

| # | Finding | AC | Verdict | Production-path evidence |
|---|---------|----|---------|--------------------------|
| R1 | cast-extract worker unregistered → AC-01/AC-02 never ran end-to-end | AC-01, AC-02 | **CLOSED** | `index.ts:156-160` registers `routeStoryboardPlanQueueJob`; `storyboardPlanQueue.processor.ts:42-55` branches on `job.name === 'cast-extract'` → `processCastExtractJob` with the **real** `castExtractJobRepo` (`workerRepositories.ts:328-409`). Job name matches enqueue side exactly (`enqueue-cast-extract.ts:14`). Repo `markCompleted` writes `truncated` into the same table the API reads (`storyboardReference.repository.ts:194-209`). Routing test `storyboardPlanQueue.processor.test.ts:46-58`; `truncated` write test `workerRepositories.test.ts:218-262`. 16/16 green. |
| R2 | `RetryAccepted.windowStatus` enum `[pending]` vs runtime `running` | AC-04 | **CLOSED** | runtime returns `running` (`blocks.service.ts:526-531,548-549`; `controller.ts:416`); enum + 202 example aligned to `running` (`openapi.yaml:1019`). |
| R3 | createBlock 429 code `flow.rate_limited` vs contract `references.rate_limited` | AC-11 | **CLOSED** | reconciled toward the shared code: `errors.ts:163` ↔ `openapi.yaml:355` both `flow.rate_limited`; decision recorded in `api-sync-report.md` (drift #5 + error-code table). |
| R4 | stale e2e docstring ("tests skip") | n/a | **CLOSED** | `e2e/storyboard-reference-flows.spec.ts:29-31` now reads "HARD-ASSERTS this wiring … fails loudly instead of silently skipping". |

## New findings (this round)

One non-blocking stage-2 nit, resolved with the owner 2026-06-07 — **Fix now**:

| # | Finding | AC | Citation | Resolution |
|---|---------|----|----------|------------|
| R5 | Stale `retryBlock` docstring said "window_status → 'pending'" while the F2/R2 fix claims the block to `'running'` — the same drift R2 fixed in the contract, lingering in the service comment. | AC-04 | `apps/api/src/services/storyboardReference.blocks.service.ts:447` | Fix now — docstring updated to "window_status → 'running', claimed so the completion hook advances — F2". Applied. |

Regression scan clean: the other five queue registrations (ingest, transcription, ai-generate, ai-enhance, storyboard-openai-image) untouched (`index.ts:54-200`); the new processor returns the handler promise directly with no try/catch, so BullMQ retry/failure semantics are preserved and errors are not swallowed; non-cast and empty-name jobs default correctly to `processStoryboardPlanJob` (`storyboardPlanQueue.processor.ts:53`, test `:60-80`); worker SQL mirrors the API repo's table/columns exactly.

## Gate result

**PASS** — all four round-2 findings (R1 blocking, R2/R3/R4 quality) genuinely closed on the production path; the single new docstring nit (R5) was fixed in-pass. The full §4 user-story set (US-01…US-08) and §5 AC set (AC-01…AC-14b) are traced end-to-end across all `target_surfaces` (api / media-worker / web-editor / e2e) across rounds 1–3, with no AC dropping out of the chain. Ready to ship.
