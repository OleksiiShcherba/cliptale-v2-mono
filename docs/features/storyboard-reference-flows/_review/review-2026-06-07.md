# Review — storyboard-reference-flows — 2026-06-07

**Verdict: CHANGES REQUESTED**

## Scope

- Diff: `9ccfb31..7d18403` (base = branch point from `master`), 34 commits, 138 files, +21 607 / −56.
- Surfaces: `apps/api` (services/controllers/repos/routes/migrations 052–055), `apps/media-worker` (jobs), `apps/web-editor` (storyboard + generate-ai-flow), `e2e/storyboard-reference-flows.spec.ts`.
- Size: L (from `.size`).
- Claimed ACs (SDD-AC trailers): the full §5 set — AC-01…AC-14b (18).
- Reviewers: 4 clean-context `sdd:reviewer` passes — AC-01..05 compliance, AC-06..14b compliance, end-to-end US/AC trace + contract fidelity, stage-2 quality.

## Trace status

- US-01…US-08: each retains ≥1 AC and a sad §6 flow; no whole-US dropout.
- Clean ACs (code + non-mocked test on every relevant surface): AC-01, AC-01b, AC-05, AC-08 (API+web), AC-08b (API), AC-10b (delete/new-scene), AC-12, AC-13, AC-14.
- Broken/partial chains: AC-02, AC-03, AC-04, AC-06, AC-07, AC-08b (worker), AC-09, AC-10, AC-11 (rate-limit clause), AC-14b — see findings. The common failure mode: behaviour exists and is tested in isolation, but the production wiring/serialization path never carries it (tests inject deps or mock the API, masking the gap).
- Clean elsewhere: auth on all 9 new routes; existence-hiding `assertDraftOwner` throughout; fully parameterized SQL; CAS on scene-link saves (409); idempotent versionless star toggle; at-most-one-primary UNIQUE; `FOR UPDATE SKIP LOCKED` claim; migrations follow repo conventions; pricing from trusted table, not LLM output; no pre-existing test weakened.

## Findings

All findings resolved with the owner on 2026-06-07: **Fix now — every one (F1–F14)** hands back to `implement` as follow-up fixes. No deferrals, no dismissals.

### Stage 1 — blocking

| # | Finding | AC | Citation | Resolution |
|---|---------|----|----------|------------|
| F1 | `GET /references/blocks` never returns `stars`/`previewFileId`/`sceneBlockIds` — `listBlocks` does no star/link enrichment, `mapBlockToWire` hard-defaults them; primary-star preview and visible linked-scene list die on canvas reload, contradicting the openapi example. e2e passes only because it mocks the whole API. | AC-06, AC-07, AC-10 | `apps/api/src/services/storyboardReference.blocks.service.ts:178`; `apps/api/src/controllers/storyboardReference.controller.ts:570`; `contracts/openapi.yaml:282-286` | Fix now |
| F2 | Rolling window never advances: `confirmCast`/`retryBlock` insert blocks as `pending` and never transition to `running`; completion-hook terminal UPDATE is guarded `WHERE window_status='running'` → 0 rows; window stalls after the first batch, `failed` status + reason never written on the real path. | AC-03, AC-04 | `apps/api/src/services/storyboardReference.confirm.service.ts:222-235,305-344`; `apps/api/src/services/storyboardReference.blocks.service.ts:370-376`; `apps/media-worker/src/jobs/ai-generate.referenceWindow.ts:82-107` | Fix now |
| F3 | `sceneReferenceSelectionRepo` not passed into `processStoryboardOpenAIImageJob` deps in the worker entrypoint → `resolveSceneInputs` short-circuits; reference boundary, scoped star gate, and derived style description never run in production. Integration test injects the repo manually, masking it. | AC-09, AC-08b (worker) | `apps/media-worker/src/index.ts:181-191`; `apps/media-worker/src/jobs/storyboardOpenAIImage.job.ts:205` | Fix now |
| F4 | AC-02 overflow notice has no end-to-end carrier: worker computes `overflow` but `markCompleted` drops it; no column; controller never emits it; UI gates on `extraction.truncated` while the contract names it `overflow`. Creator is never told entries were trimmed. | AC-02 | `apps/media-worker/src/jobs/cast-extract.job.ts:198-208,309-318`; `apps/api/src/repositories/storyboardReference.repository.ts:188-192`; `apps/api/src/controllers/storyboardReference.controller.ts:147-159`; `apps/web-editor/src/features/storyboard/components/CastConfirmModal.tsx:36-41,225-227`; `contracts/openapi.yaml:863-867` | Fix now |
| F5 | `saveSceneLinks` omits the documented `references.scene_not_in_draft` 422 validation — out-of-draft scene ids are linked silently (real foreign scene) or surface as FK 500 (nonexistent), not the contract's 422. | AC-10 | `apps/api/src/services/storyboardReference.blocks.service.ts:450-508`; `contracts/openapi.yaml` PUT scene-links 422 branch | Fix now |
| F6 | AC-14b holds only under hard delete: production draft delete is a soft delete (`deleted_at`), blocks keep `flow_id`, and `findDraftBadgesByFlowIds` does not filter soft-deleted drafts → the draft badge persists. The test issues a raw `DELETE FROM generation_drafts`, exercising a path the user-facing delete never takes. | AC-14b | `apps/api/src/services/generationDraft.service.ts:106-110`; `apps/api/src/repositories/generation-flow.repository.ts:169-188`; `apps/api/src/services/generation-flow.service.srf-t12.integration.test.ts:260-303` | Fix now |
| F7 | **SEVERE** — star endpoint accepts a cross-tenant `file_id`: the wired service validates draft-owner + block-in-draft but never that the file belongs to the block's linked flow; `files.file_id` is globally scoped, so a foreign private image can be starred and fed to scene generation. The correct `assertFileInFlow` guard lives in `generation-flow.stars.service.ts` — dead code, imported nowhere. | AC-06 / spec §6.1 | `apps/api/src/services/storyboardReference.stars.service.ts:122-161`; dead guard: `apps/api/src/services/generation-flow.stars.service.ts:84-118`; wiring: `apps/api/src/controllers/storyboardReference.controller.ts:25,493,521` | Fix now |
| F8 | Wired stars service has no genuine test: the only integration coverage targets the dead duplicate (`generation-flow.stars.service.test.ts`); the controller test mocks the service (tautology). AC-07 primary-fallback of the wired `unstarResult` is unasserted. | AC-06, AC-07 | `apps/api/src/services/storyboardReference.stars.service.ts:170,197-207`; `apps/api/src/controllers/storyboardReference.controller.t14.test.ts:754` | Fix now (merge with F7: port guard, point tests at wired service, delete the dead duplicate) |

### Stage 1 — minor

| # | Finding | AC | Citation | Resolution |
|---|---------|----|----------|------------|
| F9 | AC-10b clause "reordering changes no links" has no asserting test on any surface (the `describe` names it; no `it`). | AC-10b | `apps/api/src/services/storyboardReference.blocks.service.test.ts:624-672` | Fix now |
| F10 | AC-11 clause "bounded by existing per-user creation rate limits" unenforced/unverified: `createBlock` calls no rate-limit and no test asserts one (SAD Flow 8, `sad.md:471-476`). | AC-11 | `apps/api/src/services/storyboardReference.blocks.service.ts:242-301` | Fix now |

### Stage 2 — quality

| # | Finding | AC | Citation | Resolution |
|---|---------|----|----------|------------|
| F11 | `deleteFlow` leaks flow existence: the unscoped linked-block probe throws 409 before the ownership gate — a non-owner learns the flow exists, violating existence-hiding. | AC-12/AC-13-adjacent | `apps/api/src/services/generation-flow.service.ts:177-189` | Fix now |
| F12 | `confirmCast` embeds client-supplied `imageFileIds` into the new flow canvas with no file-ownership check (Zod validates UUID shape only). | AC-03 / spec §6.1 | `apps/api/src/services/storyboardReference.confirm.service.ts:125-145,209-218`; `apps/api/src/controllers/storyboardReference.controller.ts:34` | Fix now |
| F13 | Core e2e journeys J1/J2 are behind `test.skip(!referenceBlocksWired)` — if the canvas hook regresses, both suites silently skip and the headline ACs lose executing e2e coverage. | AC-01, 03, 06, 08, 09 | `e2e/storyboard-reference-flows.spec.ts:551-584,595,681` | Fix now (hard-assert the probe instead of soft-skip) |
| F14 | `CastConfirmModal` + `StarGateMessage` inline raw hex styles (`#ccc`, `#F87171`) instead of the repo's co-located `*.styles.ts` + shared tokens pattern (cf. `ReferenceBlockNode.styles.ts`). | n/a | `apps/web-editor/src/features/storyboard/components/CastConfirmModal.tsx:88`; `StarGateMessage.tsx:40-76` | Fix now |

## Gate result

**CHANGES REQUESTED** — 8 blocking stage-1 findings (incl. one SEVERE cross-tenant file leak), 2 minor stage-1 gaps, 4 stage-2 quality items. All 14 routed back to `implement` as fix-now follow-ups. Re-review the changed surface after the fixes land.
