# Review record — storyboard-status-block-actions

- **Date:** 2026-05-30
- **Reviewer:** independent clean-context `sdd:reviewer` (did not author the code)
- **Scope:** `git diff 3e93f9c..HEAD -- apps/ e2e/` — 15 files changed, 1562 insertions(+), 19 deletions(-)
- **Diff `SDD-AC` trailers claimed:** AC-01 … AC-09 (all nine)
- **Verdict:** **CHANGES REQUESTED** (3 minor findings, all resolved as *Fix now*)

## Stage 1 — AC / user-story traceability (the gate)

Every §4 user story and every §5 acceptance criterion traces end-to-end
(spec → §6 sequence flow → data-model → tasks → code + passing test). **No AC or user story
dropped out of the chain.** 36/36 vitest (component + hook + integration) green; e2e covers
QG-1 / QG-2 / AC-01 / AC-05 / AC-07.

| AC | Verdict | Code | Test |
|---|---|---|---|
| AC-01 regenerate scenes, leaves completed at once | SATISFIED | `StoryboardPageWorkspace.tsx` confirm → close modal → `planGeneration.start()`; `start` flips status→`queued` in `useStoryboardPlanGeneration.ts` (unmounts menu) | `StoryboardPageWorkspace.statusMenu.test.tsx`; e2e `storyboard-status-block-actions.spec.ts` (kebab gone after confirm) |
| AC-02 hide one block, sibling reflows, re-show on new cycle | SATISFIED | hide+reflow `StoryboardPageWorkspace.tsx`; re-show `useStoryboardHiddenBlocks.ts` | `StoryboardPageWorkspace.statusMenu.test.tsx`; `useStoryboardHiddenBlocks.test.ts` |
| AC-03 illustration regenerate, no modal, additive | SATISFIED | `StoryboardPageWorkspace.tsx` `handleIllustrationRegenerate` → `illustrationGeneration.start()` (no modal) | `StoryboardPageWorkspace.statusMenu.test.tsx` |
| AC-04 Ref-box removal + consistency, every viewer | SATISFIED | `StoryboardPlanControls.tsx` `{status !== 'completed' && <StoryboardReferencePreview/>}` — outside the `isOwner` gate | `StoryboardPlanControls.test.tsx` |
| AC-05 cancel/dismiss → no regen | SATISFIED | `StoryboardPageWorkspace.tsx` `handleSceneRegenerateCancel`; modal Escape/backdrop/Cancel → onCancel | `StoryboardPageWorkspace.statusMenu.test.tsx`; `StoryboardRegenerateConfirmModal.test.tsx`; e2e |
| AC-06 menu absent while in-progress/failed | SATISFIED | `StoryboardPlanControls.tsx` both menus gated `status === 'completed'` | `StoryboardPlanControls.test.tsx` (loops all non-completed states) |
| AC-07 exactly one generation on rapid trigger | SATISFIED | structural: confirm closes modal then `start()`; `start` guard ignores while queued/running/applying | `StoryboardPageWorkspace.statusMenu.test.tsx`; e2e (exactly +1 start, kebab gone) |
| AC-08 enumerate only present losses | SATISFIED | `StoryboardPageWorkspace.tsx` `sceneLosses` useMemo | `StoryboardPageWorkspace.statusMenu.test.tsx`; `StoryboardRegenerateConfirmModal.test.tsx` |
| AC-09 no kebab in DOM for non-owner | SATISFIED | render gate `StoryboardStatusMenu.tsx` `if (!isOwner) return null` (not CSS hide) | `StoryboardStatusMenu.test.tsx`; `StoryboardPlanControls.test.tsx`; `StoryboardPageWorkspace.statusMenu.test.tsx` |

| US | has AC? | has §6 flow? | reaches code+test? |
|---|---|---|---|
| US-01 Regenerate applied scenes | AC-01, AC-07 | Flow 1 | yes |
| US-02 Warned before overwrite | AC-05, AC-08 | Flow 1 + Cancel flow | yes |
| US-03 Regenerate illustrations | AC-03 | Regenerate-illustrations flow | yes |
| US-04 Hide a block | AC-02, AC-09 | Flow 2 + Re-show flow | yes |
| US-05 Consistent blocks | AC-04 | Render flow | yes |
| US-06 Act only on finished work | AC-06 | Render flow | yes |

**Stage-1 gate: PASS** — no open AC finding.

## Stage 2 — quality findings (all minor; resolved *Fix now*)

| # | Finding | file | Resolution |
|---|---|---|---|
| 1 | e2e defers the automated `@axe-core/playwright` scan that QG-2 / NFR §6 names ("dependency not yet installed"); ARIA roles + keyboard operability are asserted manually. | `e2e/storyboard-status-block-actions.spec.ts` | **Fix now** — install `@axe-core/playwright` and add an automated axe assertion on the open menu. |
| 2 | AC-02 indirect re-show is proven at the hook level but not driven end-to-end through a scene-Regenerate in the wired workspace. | `useStoryboardHiddenBlocks.test.ts` / e2e | **Fix now** — add an e2e indirect-re-show assertion (scene Regenerate restarts illustrations → previously hidden "Illustrations ready" re-shows). |
| 3 | `StoryboardPlanControls` menu props (`isOwner`/`onRegenerate`/`onHide`) are optional with `isOwner=false` default → a caller that forgets to wire the menu silently gets no menu rather than a type error. | `StoryboardPlanControls.tsx` | **Fix now** — make the menu props required so the owner gate cannot fail silently. |

No blocker or major findings. Conventions honored (inline `CSSProperties` in co-located `*.styles.ts`;
feature-local focus-trap + Escape modal per `PrincipalImageApprovalModal.tsx`; session-only React state,
no Zustand/Redux, no persistence — honors `data-model.md`; owner gate is a UX-only render gate over
unchanged server-side ownership enforcement; shared-migration rule does not fire — each new module has
one consumer).

## Gate result

**CHANGES REQUESTED** — Stage 1 clean; 3 minor Stage-2 findings to fix before ship.
Next: `/sdd:implement storyboard-status-block-actions` for the three fixes (no `/clear`), then re-review the changed surface.
