# API sync report — storyboard-status-block-actions

> **Generated by** `/sdd:api` · **Date:** 2026-05-30 · **Outcome:** **SKIPPED — no interface contract authored.**

## Decision: SKIP (no external interface authored by this feature)

This feature authors **no API contract** — not because the inputs are missing, but because the declared
surface does not own one. The skip is the surface-aware outcome, recorded here for audit.

**One-line note:** `web-frontend`-only feature that *consumes* the existing backend contract and reuses
existing generation-start paths — it introduces no new endpoint, request/response, or async event, so
there is nothing to derive. → go straight to `tasks`.

## Why skip — the two gates that decide it

### 1. Declared surface (read from `sad.md`, not re-derived)

`sad.md` frontmatter: `target_surfaces: [web-frontend]` — a **single UI surface**.

Per the surface gating table ([`_shared/surfaces.md`](../../../../.claude-work/plugins/cache/sdd/sdd/1.8.5/skills/_shared/surfaces.md) §"gating table"):

| Surface | `api` contract form |
|---|---|
| `web-frontend` | **consumes the backend contract (does not author it)** |

No `backend-service` surface is declared. A UI surface integrates against an existing contract; it does
not produce one. There is therefore no OpenAPI / gRPC / CLI / events artifact for this skill to emit.

### 2. data-model.md = N/A (no new persistence, no new backend)

`data-model.md` records **"No new persistence — no new entities, no schema change, and no migrations."**
The OpenAPI path of `api` derives every field from a `data-model.md` column; with zero columns introduced,
there is no schema to project into a contract.

## Coverage cross-check (back-feed) — every AC maps to an EXISTING/reused capability

Confirming that the skip leaves no AC stranded: each acceptance criterion resolves entirely within the
frontend or against backend paths that already exist and are unchanged by this feature.

| AC | Behaviour | Where it resolves | New contract surface? |
|---|---|---|---|
| AC-01 | Regenerate scenes (after confirm) | reuses existing `useStoryboardPlanGeneration.start` / `.retry` → existing generation backend | **No** — existing path, unchanged |
| AC-02 | Hide a completed block | session-only UI state in `StoryboardPageWorkspace` | **No** — not persisted, no server call |
| AC-03 | Regenerate illustrations (additive, no confirm) | reuses existing `useStoryboardIllustrations.start` → existing media worker + S3 | **No** — existing path, unchanged |
| AC-04 | Visual consistency / drop "Ref" box | pure render-time component change | **No** — presentation only |
| AC-05 | Cancel/dismiss warning ⇒ no regeneration | client-side dialog gate; no call issued | **No** — absence of a call |
| AC-06 | Menu unavailable while in-progress/failed | render-time guard on existing block state | **No** — reads existing state |
| AC-07 | Single generation per draft (no double-trigger) | menu removed on activation ⇒ duplicate has nothing to act on; existing backend already serializes | **No** — existing invariant |
| AC-08 | Confirmation enumerates losses; canvas rebuilt | client-side dialog + existing rebuild path | **No** — existing path |
| AC-09 | Non-owner: kebab menu not rendered | render-time owner gate (`useAuth()` id vs draft owner); existing ownership rule enforced server-side | **No** — reuses existing authz |

**Sequence-gap check:** `sad.md` §6 flows are UI-driven (`<user>` → `<ui>` → existing `<service>`); no
`alt`/`else` branch in them terminates at a *new* server error response this feature would have to define.
The only error surfaces (in-progress/failed state hiding the menu — AC-06; confirm-cancel — AC-05) are
client-side guards. **No upstream gap to fix-the-source.**

## Drift checklist

| # | Check | Result |
|---|---|---|
| — | Endpoint ↔ model | **N/A** — no endpoints authored |
| — | Error-code ↔ repo | **N/A** — no new error codes |
| — | Validation ↔ constraint | **N/A** — no new request fields |
| — | OpenAPI ↔ sequence | **N/A** — no OpenAPI document |
| ✓ | Every §5 AC maps to an existing/reused capability (no new contract surface) | **Pass** (table above) |
| ✓ | Declared surface authors no contract (`web-frontend` consumes only) | **Pass** |
| ✓ | data-model introduces no schema to project | **Pass** |

## Definition of Done (skip variant)

- [x] Interface kind read from `sad.md` `target_surfaces`, not re-derived.
- [x] Skip decision recorded with a one-line note + the two deciding gates.
- [x] Back-feed coverage cross-check: all 9 ACs map to existing/reused capabilities; none needs a new contract.
- [x] No `openapi.yaml` / `events.md` written (correctly — nothing to author).

## Run next

```
/clear
/sdd:tasks storyboard-status-block-actions
```
