# Review record — generate-ai-flow — 2026-06-04 (pass 17 — re-review of the pass-16 fix surface)

**Reviewer:** independent clean-context pass (2 fan-out reviewers: U4 surface, U5 surface), merged.
**Gate result:** **PASS**

## Scope

- Diff: commits `c63e359` (U4) + `61375a0` (U5) on `feat/generate-ai-flow` — the surface review-2026-06-04-pass16.md demanded fixed; the V1 follow-up fix `5a71b6a` landed within this pass.
- Prior baseline: pass-16 record (Part A: U1–U3 clean). Cleanly-verified areas are not re-litigated.

## Part A — U4 (flow card click-to-open, Open removed): **SATISFIED / REVIEW_CLEAN**

- Every demanded element lands, point-for-point consistent with the `ProjectCard` witness: card root `role="button"` + `tabIndex` + `aria-label` + click/Enter/Space → navigate (`FlowListPage.tsx:167-170,128-134`), JS-hover border highlight + `transition` (`:119,172-184`), Open button removed (test pins `queryByText('Open')` null), `stopPropagation` on Rename (`:248`) / Delete (`:271`) / rename-input (`:194`), navigation no-op while renaming (`:124`).
- A11y verified: no duplicate accessible names; the keydown guard `e.target !== e.currentTarget` (`:129`) keeps Enter/Space typed in inner controls from hijacking the card; no focus-style regression vs the witness.
- Tests: 14/14 — all six required behaviors pinned. No new findings.

## Part B — U5 (multi-result run history): **SATISFIED**, one new finding (fixed in-pass)

All five demanded elements land (cited by the reviewer): `params.jobId` binding (`useFlowCanvas.ts:309-316`); always-append with stack-above positioning (`FlowEditorPage.tsx:218-254`); jobId re-keying + legacy latest-per-sourceGen fallback (`:141-154, :314-339, :376-391`, `findResultBlock :483-501`); e2e rewritten (failed-retained reload + Generate-twice); all artifact amendments match the code (spec AC-01/AC-09, sad §12, data-model, test-plan, repo comment).

Hard-spot checks all clean: no double-create (deps change once per run); legacy freeze binds the correct prior job (functional `setCanvas` reads fresh state); a stale reattach seed cannot overlay a block bound to another run (`liveMatches` guard); per-run reload resolution + the `needed` preview filter exact; `jobId` survives the autosave round-trip (canvas signature includes params; backend Zod `z.record` accepts it).

### V1 — same-session regenerations stacked at the SAME Y (visual overlap) — **FIX NOW → FIXED**

- **Sev:** MINOR (positional only — AC-01's retained-history/binding/no-collapse all held). **AC:** AC-01 positioning clause ("newest on top").
- **Finding:** the tracking effect read `controllerRef.current.canvas` — a snapshot frozen at mount (`FlowCanvas.tsx:87-92` fires `onCanvasReady` once; the captured object's `.canvas` is the first-render state) — so a 2nd in-session run computed `y = min(loadTimeResults.y) − 280`, the same Y as the 1st append → overlap. Unpinned because no test ran two regenerations in one session.
- **Resolution (owner, 2026-06-04): Fix now.** Fixed in `5a71b6a`: the effect reads the live streamed canvas via `canvasLiveRef` (`FlowEditorPage.tsx`); a new unit test pins two same-session regenerations landing at distinct Y (−280, then −560) with the prior block retained at 0. Bonus: `genBlk`/`alreadyBound` now also see session-added blocks.
- **Gate after fix:** web feature 125/125; web tsc 131 = baseline; e2e 6/6 GREEN live.

## Resolution summary

| # | Verdict | Route |
|---|---------|-------|
| U4 | SATISFIED | clean — no findings |
| U5 | SATISFIED | clean on all demanded elements |
| V1 | Fix now → **fixed in-pass** | `5a71b6a` (live-canvas ref + Y-stacking test) |

No open finding remains. End-to-end AC chain: traced fully in review-2026-06-03 + pass-15/16; this pass re-verified the only changed surface.

**Verdict: PASS** — ready for `/sdd:ship generate-ai-flow` (the live a–s manual journey from review-2026-06-03 "Next" remains the ship-time gate; F8–F11 stay deferred in spec §8 with owner+due).
