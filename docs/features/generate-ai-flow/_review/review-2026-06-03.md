# Review record — generate-ai-flow — 2026-06-03

**Reviewer:** independent clean-context pass (3 fan-out reviewers: stage-1 AC trace, stage-2 backend/worker/domain, stage-2 frontend), merged.
**Gate result:** **CHANGES REQUESTED**

## Scope

- Diff: `git diff a2f972b..HEAD` on `feat/generate-ai-flow` (base = merge-base with master `a2f972b`).
- 111 files changed, +15819 / −11.
- Surfaces (sad.md `target_surfaces`): web-frontend, backend-service, worker.
- Baseline read: spec.md §4/§5/§6/§6.1, sad.md §6, data-model.md, contracts/openapi.yaml, adr/0001–0007, tasks.json, test-plan.md.

## AC traceability summary

All 21 ACs (AC-01…AC-19 incl. AC-08b, AC-10b) and 7 user stories were traced end-to-end (spec → §6 sequences → data-model → openapi → tasks → code+test). Every AC maps to ≥1 task and every US has ≥1 AC + a §6 flow. Coverage is genuine for 15/21 through the rendered UI / real-DB integration / worker integration. Six findings (below) mark where the chain breaks or the spec'd UI behaviour is satisfied backend-only.

Note: E2E suite is 4/4 green, but its happy path runs on a freshly-loaded, un-edited flow with the default model and a text content block — so it does not exercise the stale-version, model-picker, or multi-modality paths the findings cover. This is exactly the cross-cutting gap a per-task gate cannot see.

## Findings & verdicts

### Fix now (handed back to /sdd:implement)

| # | Sev | AC | Finding | Cite | Verdict |
|---|-----|-----|---------|------|---------|
| F1 | BLOCKER | AC-01, AC-10b | Generate sends stale `flow.version` after any autosave (autosave bumps server version → `localVersion`, but `confirm()` is seeded with the loaded `flow.version`) → 409, generation silently fails once the canvas is edited. Pass `localVersion` instead. | `FlowEditorPage.tsx:152-157`, `useFlowAutosave.ts`, `openapi.yaml:576-579` | **FIX NOW** |
| F2 | BLOCKER | AC-07, AC-15 | Model selection/change unreachable in assembled UI — Inspector renders only optional params, no model picker; `useFlowCanvas.changeModel`/`reconcileModelChange` is unit-tested dead code; AC-15 "select a model" only satisfiable via hardcoded `DEFAULT_MODEL_ID`. Add a model `<select>` in the generation-block branch → `changeModel`. | `Inspector.tsx:112-124`, `GenerationNode.tsx:86`, `useFlowCanvas.ts:324-335`, `FlowEditorPage.tsx:60,211,281` | **FIX NOW** |
| F3 | MAJOR | AC-15, AC-13 | "Add content" hardcodes `addContentBlock('text', …)`; image/audio/video content blocks unreachable, so AC-13 (image→video) cannot be assembled. Offer the four modalities. | `FlowEditorPage.tsx:209` | **FIX NOW** |
| F4 | MAJOR | AC-03, AC-05, AC-06, AC-17 | Gate (422) and stale-version (409) errors never rendered — `generation.error` is set in the hook but never referenced by the page; the cost modal just stays open with no message. Spec mandates "tell the Creator, in plain language." Render `generation.error` (e.g. `role="alert"` in modal) + add a component test that a 422 surfaces its message. | `useFlowGeneration.ts:146-150`, `FlowEditorPage.tsx`, `CostConfirmModal.tsx` | **FIX NOW** |
| F5 | MAJOR | AC-19 | Soft-delete (`deleteFlow` sets `deleted_at`) never drops the `flow_files` linkage — `ON DELETE CASCADE` never fires on soft-delete and `softUnlinkFileFromFlow` is called nowhere on the delete path; "only the flow linkage is dropped" unmet (asset-preservation half holds). Call `softUnlinkFileFromFlow` inside `deleteFlow` (same txn) + integration test: after delete, links gone / files remain. | `generation-flow.service.ts:129-134`, `flow-file.repository.ts:32`, `flow-file-repository.integration.test.ts:177-202` | **FIX NOW** |
| F6 | MAJOR | AC-10, §6.1, ADR-0002 | Canvas persisted without `flowCanvasSchema.parse()` — `saveCanvas` accepts `z.record(z.unknown())` and `JSON.stringify`s straight to the column; an invalid canvas flows untrusted into the gate/job. Parse in `saveCanvas` → `ValidationError` (400). | `generation-flow.service.ts:154`, `generation-flow.controller.ts:38-41`, `generation-flow.repository.ts:221`, `flowCanvas.schema.ts:9` | **FIX NOW** |
| F7 | MAJOR | AC-05, AC-17 | Asset gate ignores `files.status` — a `failed`/`processing` asset passes `assertAssetUsable`, so an empty/broken input reaches the paid provider. Treat non-ready/failed status as not-usable → `ContentInvalidError`/`AssetMissingError`. | `flow-generate.service.ts:230-249`, `file.repository.ts:227` | **FIX NOW** |

### Deferred → spec §8 Open questions (owner + due)

| # | Sev | AC | Finding | Cite | Verdict |
|---|-----|-----|---------|------|---------|
| F8 | MINOR | AC-17 | "Unsupported file type or size" not enforced — gate validates modality+empty only; `bytes`/`mimeType` unused. | `flow-generate.service.ts:288-298` | **DEFER** |
| F9 | MINOR | AC-01 | Idempotency "pending" loser surfaced as 429 `RateLimitedError` instead of a 409 conflict (functionally safe, mislabels the situation). | `flow-generate.service.ts:514-518` | **DEFER** |
| F10 | MINOR | — | Confirm button never disabled in-flight — `submitting={false}` hardcoded; double-click possible (deduped server-side by idem-key). | `FlowEditorPage.tsx:356`, `CostConfirmModal.tsx:120` | **DEFER** |
| F11 | MINOR | AC-08b | Test-adequacy: AC-08b reattach unit test exercises the `initialJobState` seam, which the assembled page does not use on cross-session reopen (real path = polled `flow.jobs`, covered by e2e). | `useFlowGeneration.test.ts:124-142`, `FlowEditorPage.tsx:148-150` | **DEFER** |

## Verified clean (so re-review does not re-litigate)

- Owner-scoping enforced on every flow read/write/list/delete/estimate/generate (`WHERE user_id=?` + existence-hiding 404); AC-05-vs-AC-04 distinction precise (previously-owned→422 / never-owned→404).
- Rate limit server-side, Redis Lua, per-Creator, un-bypassable; cost gate + version-409 run before any spend.
- Result-integrity holds — `flow_files` written only in the worker success branch after `setOutputFile`; single first-output kept, extras discarded (AC-14); none on failure (AC-09).
- AC-01/08/08b/10/10b traced through the rendered UI (e2e green); AC-12 (audio) / AC-13 (image→video) genuinely exercised, not image-only.

## Fixes applied (implement pass, 2026-06-03)

F1–F7 implemented test-first (RED→GREEN), committed on `feat/generate-ai-flow`. F8–F11 deferred to spec §8 per the resolution.

| # | Commit | Test (RED→GREEN) | Gate |
|---|--------|------------------|------|
| F5 | `39481e2` deleteFlow drops flow_files linkage | `generation-flow.service.test.ts` (unit: unlink called / not-on-non-owner) + `generation-flow.service.integration.test.ts` (real MySQL: link soft-deleted, asset row alive) | unit+integration green |
| F6 | `9f39e8e` saveCanvas parses flowCanvasSchema → ValidationError | `generation-flow.service.test.ts` (invalid canvas rejected before repo write) | green |
| F7 | `9f39e8e` asset gate blocks non-`ready` status | `flow-generate.service.test.ts` (processing asset → ContentInvalidError) | green |
| F1 | `79e7324` Generate uses autosave `localVersion` | `FlowEditorPage.test.tsx` (edit→autosave v3→v4→generate sends v4) | green |
| F2 | `79e7324` Inspector model picker → changeModel | `Inspector.test.tsx` (picker default + onModelChange) + `FlowEditorPage.test.tsx` (select-model→change persists) | green |
| F3 | `79e7324` Add-content menu (4 modalities) | `FlowEditorPage.test.tsx` (text + image/audio/video menu adds) | green |
| F4 | `79e7324` render `generation.error` in modal + page alert | `CostConfirmModal.test.tsx` (error role=alert) + `FlowEditorPage.test.tsx` (422 surfaces message) | green |

**Gate snapshot:** backend flow unit + integration **73 passed**; frontend `generate-ai-flow` suite **78 passed**; `tsc --noEmit` clean for all touched files. Lint is non-functional repo-wide (ESLint v9 vs absent legacy config — pre-existing infra). **E2E not re-run in this pass** (needs a live server + seeded login under a 15-min rate limit) — its toolbar isn't exercised by the spec (the flow is network-stub-seeded, driven by `data-block-id`), and the happy path doesn't edit-then-generate, so the changed surfaces are not covered by the existing E2E. **Re-run E2E + add an edit→generate and a gate-error-message assertion during re-review.**

## Re-review pass 2 (2026-06-03) — CHANGES REQUESTED → fixed

Independent clean-context reviewer re-ran over the whole diff after the F1–F7 + home-tab fixes, prompted by a runtime crash the owner hit in manual testing (`assets.filter is not a function` when picking an image). F1–F7 + home/routing confirmed holding (no regressions). One new BLOCKER cluster + a MINOR, all **Fix now** (owner-resolved), now implemented:

| # | Sev | AC | Finding | Cite | Fix (commit `650f1a9`) |
|---|-----|-----|---------|------|------------------------|
| G1 | BLOCKER (stage-2) | AC-16, US-04 | Media content path wired to a placeholder `{kind:'project', id:'__flow__'}` the Creator doesn't own → library-pick crashed (`assets.filter`), upload silently no-op'd (link to a fake project failed, `onUploadComplete` never fired). | `ContentInput.tsx:31,160`, `AssetPickerField.tsx:58`, `useFileUpload.ts:50-55` | Added `kind:'library'` to `AiGenerationContext`+`UploadTarget`; `getContextAssets('library')`→`GET /files` (adapts FileSummary→AssetSummary); library upload target skips the link step (file already in the library after finalize); ContentInput uses the library context+target. |
| G2 | BLOCKER (stage-1) | AC-16, US-04 | AC-16 media covered only by mocked tests — `Inspector.test.tsx` mocked `AssetPickerField`+`useFileUpload`, hiding the broken seam; E2E ran text→image only. | `Inspector.test.tsx:25-42`, `e2e/...:367` | New `ContentInput.library.test.tsx` renders the REAL `AssetPickerField` against a stubbed `/files` (no throw + a pick writes `fileId`); unit tests for the `getContextAssets('library')` adapter and the library upload no-op. |
| G3 | MINOR (stage-2) | AC-13/16 | A video content block listed image assets (`AssetPickerMediaType` was `image\|audio`). | `ContentInput.tsx:222`, `AssetPickerField.tsx:14` | `AssetPickerMediaType` gains `'video'`; picker labels generalised; ContentInput drops the video→image coercion. |

**Pass-2 gate snapshot:** affected web-editor suites **396 passed** (shared/ai-generation + shared/file-upload + generate-ai-flow + home); `tsc --noEmit` introduces zero new errors in touched files (the one residual `AiGenerationPanel` `AiModel/FalModel` line is pre-existing, unrelated). Lint still non-functional repo-wide (ESLint v9 infra). **E2E still not re-run** (live server + seeded login / 15-min rate limit).

**Verdict: PASS for the reviewed code surface** — every §4 US / §5 AC now reaches code + a real (non-mocked) test across the declared surfaces; AC-16's media half, previously satisfied only by a placeholder + mocks, is now genuinely wired and covered. The sole remaining gate item is **live E2E verification** (add an asset-block upload/pick + an edit→generate + a gate-error-message assertion) — to be run during `/sdd:ship` since it needs a running stack.

## Re-review pass 3 (2026-06-03) — owner-reported canvas UX gaps → fixed

Two concrete UX defects from manual testing, both **Fix now** (owner-resolved), implemented TDD in commit `4f10c0d`:

| # | Sev | AC | Finding | Cite | Fix |
|---|-----|-----|---------|------|-----|
| H1 | MAJOR (stage-2) | AC-15 / US-02 | No way to delete a block — `FlowCanvas.onNodesChange` handled only `position` changes and dropped xyflow `remove` events, and no delete affordance existed. | `FlowCanvas.tsx:141-164` | `useFlowCanvas.removeBlock`/`removeEdge` (pure `removeBlockFromCanvas`/`removeEdgeFromCanvas`, unit-tested — block delete also drops incident edges); a × button in every node header (shared `NodeDeleteButton` via `FlowExtras.nodeActions`); FlowCanvas applies node + edge `remove` changes (Delete/Backspace). |
| H2 | MINOR (stage-2) | US-02 | Inspector stayed open when clicking the empty canvas. | `FlowCanvas.tsx:173-178` | FlowCanvas `onPaneClick` → FlowEditorPage clears the selection (block click still selects). |

**Pass-3 gate:** generate-ai-flow suite **93 passed** (incl. new pure-helper, NodeDeleteButton, delete-via-×, and pane-click-closes-Inspector tests); `tsc --noEmit` 0 new errors in touched files. Verdict unchanged: **PASS for code surface**; live E2E still the only remaining gate item.

Considered but de-scoped (not raised as findings): a delete-confirmation prompt (cheap to add later if accidental deletes become a problem); undo for deletes (already deferred via §8 undo/redo open question).

## Re-review pass 4 (2026-06-03) — owner-reported clear-asset bug → fixed

| # | Sev | AC | Finding | Cite | Fix (`e87c293`) |
|---|-----|-----|---------|------|-----------------|
| I1 | MAJOR (stage-1) | AC-16 / US-04 | Removing the image from an image content block did nothing — the picker's Clear (×) calls `onChange(undefined)`, but `handleLibraryPick` only acted on a non-empty string, so the block kept its `fileId`. | `ContentInput.tsx:180-184`, `AssetPickerField.tsx:83` | `handleLibraryPick` now propagates a clear as an empty `fileId` → ContentNode shows "No asset selected", gate treats input as empty. Component test added. |

**Pass-4 gate:** generate-ai-flow suite **94 passed**; tsc 0 new errors. Verdict unchanged: **PASS for code surface**; live E2E remains the only gate item. (Note: a stale `node_modules/.vite` cache produced a spurious "Failed to load AssetPickerField" during this pass — cleared; not a code defect.)

## Re-review pass 5 (2026-06-03) — owner-reported inspector picker UX → fixed

| # | Sev | AC | Finding | Cite | Fix (`d426386`) |
|---|-----|-----|---------|------|-----------------|
| J1 | MINOR (stage-2) | AC-16 / US-04 | The library picker required a "Pick…" button click before showing options. | `AssetPickerField.tsx:51,121-129`, `ContentInput.tsx` | Additive `defaultOpen` prop (ContentInput passes it) shows the list immediately; the prompt button only appears when the list is closed. |
| J2 | MINOR (stage-2) | AC-16 / US-04 | A selected image had no preview in the inspector value chip (text only). | `AssetPickerField.tsx:108-129` | The bulk thumbnail resolver now also covers the current selection; the single-mode chip renders a small thumbnail for the selected image. |

**Pass-5 gate:** **576 passed** across shared/ai-generation + file-upload + generate-ai-flow + generate-wizard (the wizard also consumes AssetPickerField; both changes are additive + backward-compatible so it is unchanged); tsc 0 new errors. Verdict unchanged: **PASS for code surface**; live E2E remains the only gate item.

## Re-review pass 6 (2026-06-03) — provider 422 on real generation → fixed

A real Generate failed at fal.ai (422 `list_type` on `image_urls`) — surfaced by the owner. **Fix now** (stage-1 correctness), implemented in `b8a4407`:

| # | Sev | AC | Finding | Cite | Fix |
|---|-----|-----|---------|------|-----|
| K1 | BLOCKER (stage-1) | AC-01/AC-13/AC-16 | The enqueue path sent a content block's raw **file id** as the provider input (not a presigned URL), and put a single string into `image_urls` (an `image_url_list` field that requires an array). The comment claimed "worker resolves to a URL" but the worker forwards `options` verbatim — so generations with an image input always failed at the provider. | `flow-generate.service.ts:454-485` (`buildJobOptions`), `:555` (`generate`), `aiGeneration.assetResolver.ts`, `media-worker ai-generate.job.ts:174` | `buildJobOptions` now collects all edges per handle and emits an **array** for `image_url_list`; `generate` runs the existing `resolveAssetImageUrls` before createJob+enqueue, rewriting image/audio file ids → short-lived presigned HTTPS URLs (the same resolver the wizard uses). |

**Pass-6 gate:** flow-generate **unit 32 + integration 12** green (the integration runs the real resolver presigning against test S3); tsc clean. This was a genuine end-to-end gap the prior passes missed because no test drove `buildJobOptions`/the enqueue payload with a connected image asset — now covered. Verdict: **PASS for code surface**; live E2E (with a real image-input generation) remains the final gate item.

## Re-review pass 7 (2026-06-03) — completed-generation UX → fixed

Two defects from manual testing of a completed generation, both **Fix now**, implemented in `5310fe1`:

| # | Sev | AC | Finding | Cite | Fix |
|---|-----|-----|---------|------|-----|
| L1 | MAJOR (stage-1) | AC-08 | The result image preview was broken — `getFileUrl` hit `GET /files/:id` (no such route → 404), fell back to `/assets/:id` (`{downloadUrl}`, not `{url}`), and resolved to null. | `api.ts:137` vs `file.routes.ts` (only `/files/:id/stream` returns `{url}`) | `getFileUrl` now hits `GET /files/:id/stream` — the same endpoint the content preview (`useFileStreamUrl`) uses. |
| L2 | MAJOR (stage-1) | AC-01 | The auto-created result block had only `params.sourceBlockId`, no edge, so it looked disconnected from its generation block. | `FlowEditorPage.tsx:188-197` | `useFlowCanvas.addResultBlock` creates the result block AND the visible gen `out`→result `in` edge; FlowEditorPage uses it on Generate + "Add result". Edge persists via autosave/reload. |

**Pass-7 gate:** generate-ai-flow suite **98 passed** (new: `getFileUrl` targets the stream endpoint; Generate persists a gen→result edge to the saved canvas); tsc clean. Verdict: **PASS for code surface**; live E2E remains the final gate.

> Pattern note: passes 2–7 were all runtime defects in the assembled UI / provider integration that surfaced only under a live stack — none catchable by the unit/component suites alone. **A live E2E run (steps a–j) is now a hard precondition for ship, not an optional verification.**

## Re-review pass 8 (2026-06-03) — canvas selection feedback + edge delete gesture → fixed

Two canvas-editing UX asks, both **Fix now**, implemented in `9f48eba`:

| # | Sev | AC | Finding | Cite | Fix |
|---|-----|-----|---------|------|-----|
| M1 | MINOR (stage-2) | AC-15 / US-02 | Clicking a block or edge gave no visual selection feedback — custom nodes ignored xyflow's `selected` prop, and since the nodes array is derived from the canvas doc (not xyflow's internal state) selection never reached them. | `ContentNode/GenerationNode/ResultNode` (no `selected` use), `FlowCanvas.tsx` nodes/edges memo | Node `selected` driven from `selectedBlockId`; nodes render a PRIMARY outline (`nodeSelectedOutline`); ephemeral `selectedEdgeId` highlights a clicked edge (clears block selection); node/pane click clears it. |
| M2 | MINOR (stage-2) | AC-15 / US-03 | No pointer way to remove a connection (keyboard-only Backspace, undiscoverable). | `FlowCanvas.tsx` (no `onEdgeDoubleClick`) | `onEdgeDoubleClick` → `removeEdge` deletes the connection; keyboard Delete still works. |

**Pass-8 gate:** generate-ai-flow suite **101 passed** (new: node renders the outline only when selected; removeEdge drops a connection through a live FlowCanvas). The double-click gesture itself is **E2E-verified** — xyflow does not render edge geometry in jsdom, a known limitation; tsc clean. Verdict: **PASS for code surface**.

## Re-review pass 9 (2026-06-03) — result persistence + canvas drag jitter → fixed

Two defects from manual testing, both **Fix now**, implemented in `a9f9672`:

| # | Sev | AC | Finding | Cite | Fix |
|---|-----|-----|---------|------|-----|
| N1 | MAJOR (stage-1) | AC-10 / AC-08b | A produced result image vanished after a page refresh. `flow.jobs` are keyed by the GENERATION block (`setFlowLink` → `job.block_id`), but the result block resolved its job/preview by its OWN id; live worked via the `liveResultBlockId` overlay, reload found nothing. | `FlowEditorPage.tsx` `result()` + `reattachState` | `result()` resolves job + preview through the result block's `sourceBlockId` (the generation block); reattach seed keyed by the generation block. |
| N2 | MAJOR (stage-2) | AC-15 / US-02 | Dragging a block made all edges jump — `onNodesChange` wrote every position tick back into the canvas doc, re-deriving the whole nodes array (resetting `initialWidth/Height`) → handle anchors moved → edges jittered. | `FlowCanvas.tsx` old `nodes` memo + `onNodesChange` | FlowCanvas now lets xyflow OWN the rendered nodes (`applyNodeChanges`): structure/selection/data reconciled from the doc without clobbering live positions/measurements; final position committed to the doc on drag stop; edges (from unchanged `canvas.edges`) stay stable. |

**Pass-9 gate:** generate-ai-flow suite **102 passed** (new: FlowEditorPage restores the result image on reload via sourceBlockId; FlowCanvas node render still green through the rfNodes refactor). The drag smoothness itself is **E2E-verified** (jsdom does not run xyflow drag/measurement). tsc 0 new errors (131 = baseline). Verdict: **PASS for code surface**.

## Re-review pass 10 (2026-06-03) — stale failed run shown on reload → fixed

| # | Sev | AC | Finding | Cite | Fix (`3268a4c`) |
|---|-----|-----|---------|------|-----------------|
| O1 | MAJOR (stage-1) | AC-10 / AC-09 | After refresh the result block showed an OLD failed run (the pre-fix fal 422) even though a later retry succeeded. A block can have several jobs; `flow.jobs` was unordered and `jobsByBlock[blockId]=j` kept an arbitrary one, so a stale failure overrode the success. | `getJobsByFlowId` (no ORDER BY), `FlowEditorPage.tsx` jobsByBlock | Backend `getJobsByFlowId` ORDER BY created_at ASC; `toJobState`+openapi expose `createdAt`; frontend `jobsByBlock` keeps the LATEST run per block and the preview resolves that run. |

**Pass-10 gate:** web feature **103** + api flow **64** green (new: controller maps `createdAt`; FlowEditorPage shows the newer successful run on reload, not the older failed one); tsc clean both packages. Verdict: **PASS for code surface**.

> This is the third reload/persistence defect (N1, O1) and the second fal-422 manifestation — all only visible against a live DB with real run history. The recommendation stands: **a live E2E pass with a real failed-then-succeeded run history is required before ship.**

## Re-review pass 11 (2026-06-04) — DB status enum mismatch: 100% bar instead of the image → fixed

| # | Sev | AC | Finding | Cite | Fix (`ff73a88`) |
|---|-----|-----|---------|------|-----------------|
| P1 | MAJOR (stage-1) | AC-08b / AC-10 | After refresh the result block showed a stuck **100% progress bar** while the generated image sat in the library. Root cause: the DB enum is `queued|processing|completed|failed` (migration 014, `aiGenerationJob.repository.ts:6`; `setOutputFile` writes `'completed'`) and the controller passes it **verbatim** — but the frontend invented `JobStatusEnum = 'queued'|'running'|'done'|'failed'`. The remaps in `jobStateToJob` / `jobStateToAiJob` only handled `'done'`, so a real `'completed'` fell into the default → `'queued'` → ResultNode rendered the progress branch at `progress=100`; the preview effect (`status !== 'done'`) never resolved the URL either. Unit fixtures used `'done'`, so the suite stayed green while every real reload was broken. | `types.ts:69`, `FlowEditorPage.tsx` jobStateToJob + preview effect, `useFlowGeneration.ts` toAiJobStatus, openapi `JobStatusEnum` (which claimed "verbatim, not invented" while listing invented values) | `JobStatusEnum` → verbatim DB values; both remaps become pass-throughs; preview effect keys on `'completed'`; `anyPending` checks `'processing'`; openapi enum corrected; fixtures updated to real statuses + a regression test asserting **image, not progress bar**, for status `'completed'`. |

**Pass-11 gate:** web feature **104** + api flow **64** green; tsc 0 new errors (131 = baseline). Verdict: **PASS for code surface**.

> Pass-9's N1 fix was verified with a `'done'` fixture — i.e. the test encoded the same wrong contract as the code, which is why reload kept failing live across three passes. Lesson recorded: reattach fixtures must use DB-verbatim values. This is exactly the defect class only a live E2E catches; the a–o checklist below remains the ship gate.

## Re-review pass 12 (2026-06-04) — music generation accepts no input edge → fixed

| # | Sev | AC | Finding | Cite | Fix (`80fadd6`) |
|---|-----|-----|---------|------|-----------------|
| Q1 | MAJOR (stage-1) | AC-06 / AC-15 | The music-generation block rendered "No required inputs" — no input handle, so no edge could connect. Root cause: handles are derived only from fields that are `required` OR in an `exclusiveGroup` (`useFlowCanvas.ts:90`); the music model's `prompt` is `required:false` (a composition plan is the alternative) and NEITHER field carried an `exclusiveGroup` — unlike the fal models' `prompt_mode` annotation. Deeper: the generate gate's required+XOR passes were both empty for music, so a zero-input music Generate passed the gate → a charged provider call guaranteed to fail. | `elevenlabs-models.ts:247` (music inputSchema), `useFlowCanvas.ts:90-107`, `flow-generate.service.ts:361-387` | `prompt` + `composition_plan` annotated `exclusiveGroup: 'prompt_mode'`: the canvas renders a text handle for prompt (composition_plan stays inspector-only — no media modality, same as kling's `multi_prompt`), and the gate now enforces exactly-one-of server-side. |

**Pass-12 gate:** api-contracts **219** + web feature **105** + api services flow tests green; tsc baseline (131). NOTE: 5 unrelated api `*.restore.service` tests fail — a pre-existing calendar time-bomb (fixtures hardcode `deletedAt: 2026-04-10` "within TTL", now >30 days old); out of scope for this branch, fix the fixtures to relative dates separately. Verdict: **PASS for code surface**.

## Re-review pass 13 (2026-06-04) — result placement + audio block width + the restore-test calendar mine → fixed

Three user-reported findings, all **Fix now**:

| # | Sev | AC | Finding | Cite | Fix |
|---|-----|-----|---------|------|-----|
| R1 | MINOR (stage-2) | AC-15 / US-02 | A result block added from the toolbar cascaded from the TOP-LEFT (`nextPos()`), not to the right of its generation block — only the auto-created-on-Generate path placed it right of the gen block. The two paths also duplicated the magic `+320`. | `FlowEditorPage.tsx` old `handleAddResult` (used `nextPos()`) vs the auto-create effect (`+320`) | Shared `RESULT_OFFSET_X = 320`: BOTH paths place the result at `{gen.x + RESULT_OFFSET_X, gen.y}` — always right of its source generation block with a small gap; `nextPos()` remains only as the no-generation-block fallback. |
| R2 | MINOR (stage-2) | AC-12 / US-02 | The music (audio) result block was as narrow as any node (`minWidth: 200`), leaving the `<audio>` timeline scrubber ~176px — too coarse to control the listening position within a generated track. | `ResultNode.tsx` (single `nodeRoot`), `flowNodeStyles.ts:28` | `audioNodeRoot` (`minWidth: 380`) applied when `modality === 'audio'`; non-audio results keep the default width (regression-tested both ways). |
| R3 | MAJOR (stage-2, out-of-feature-diff) | — (repo test gate) | The calendar mine flagged in pass 12 detonated: 4 `*.restore.service` api tests failed because fixtures hardcoded `deletedAt: 2026-04-10` («within TTL») — now >30 days old, so the happy-path fixtures hit the `GoneError` TTL branch. Pre-existing on master (not this feature's diff), but it breaks the api gate this branch must pass. | `project.restore.service.test.ts:30`, `generationDraft.restore.service.test.ts:35`, `project.restore.service.ts:51` | Time anchored with `vi.useFakeTimers()` + `vi.setSystemTime('2026-04-20')` — the same idiom `file.softdelete.service.test.ts:87-95` already uses — so the fixture is permanently «10 days ago, within TTL». |

**Pass-13 gate:** web feature **108 passed** (new: toolbar Add result lands at `gen.x + 320, gen.y`; audio result node ≥380px wide; non-audio stays 200) + api restore suites **12 passed** (previously 4 failed). eslint not runnable repo-wide (pre-existing flat-config migration breakage, both apps). Verdict: **PASS for code surface**.

## Re-review pass 14 (2026-06-04) — block move autosaves + music length is one honest control → fixed

Two user-reported findings, both **Fix now**:

| # | Sev | AC | Finding | Cite | Fix |
|---|-----|-----|---------|------|-----|
| S1 | MAJOR (stage-1) | AC-10 / AC-10b | Moving a block never triggered autosave — `contentSignature` deliberately excluded positions (pass-9 churn guard), so a pure drag never streamed the canvas and the re-arranged layout was lost on reload. The guard was obsolete: since pass 9 the doc receives a position only on the final drag-stop commit (xyflow owns live drag), so there is no per-tick churn to guard against. | `FlowCanvas.tsx:94-107` (old signature), `FlowCanvas.tsx:202-205` (comment admitting the gap) | Positions join the signature → a move = exactly one debounced autosave (`69accef`). |
| S2 | MAJOR (stage-1) | AC-06 / AC-12 | Music generation did not respect the configured length, and the Inspector showed TWO overlapping controls — 'Music Length (ms)' (3,000–600,000) and legacy 'Duration (seconds)' (3–600) — with a silent ms-wins precedence (`getMusicLengthMs`), so a set Duration could be ignored with no signal. Deeper: the flow path always composes via a composition plan, where length is carried by the plan's section durations — and `respect_sections_durations` (catalog default TRUE) was sent only when defined; the Inspector never prefills defaults, so the compose omitted it and ElevenLabs drifted from the requested length. | `elevenlabs-models.ts:276-292` (two fields), `ai-generate-audio.handler.ts:295-302` (precedence) + `:184` (no `?? true`, unlike `force_instrumental` at `:185`), `elevenlabs-client.ts:317-322` (field omitted when undefined) | Owner decision: **one seconds field**. Catalog gains a `hidden` field flag (`fal-models.ts`): `music_length_ms` hidden (kept for storyboard-music callers), `duration` relabeled **'Length (seconds)'** as the one visible control (worker already converts ×1000 for plan creation); Inspector filters hidden fields; worker falls back `respect_sections_durations ?? true` (`d2dc850`). |

**Pass-14 gate:** api-contracts **219** + media-worker jobs **168** + web feature **110** + api flow-generate **32** — all green (new: drag-stop commit streams the canvas; music model exposes a single 'Length (seconds)' and hides `music_length_ms`; seconds field drives plan length at ×1000 and compose defaults `respect_sections_durations=true`). Verdict: **PASS for code surface**.

> Answer to the owner's question «в чому різниця між music length і duration»: різниці немає — `duration` був legacy-полем, що конвертується в `music_length_ms`; тепер на канвасі залишилось одне поле 'Length (seconds)'.

## Next

`/sdd:ship generate-ai-flow` — and as part of ship verification, run the E2E with: (a) add an image content block → pick/upload a library asset, (b) edit-then-Generate, (c) assert the plain-language gate-error message renders, (d) add then delete a block (× + Delete), (e) click the pane → Inspector closes, (f) pick an image then Clear (×) → "No asset selected", (g) select a content block → library list shown without a click + selected thumbnail, (h) run a real image→image/video generation → provider gets presigned URL arrays (no 422), (i) completed result renders its image preview, (j) result block drawn connected to its generation block, (k) click a block/edge → highlights, (l) double-click an edge → removed, (m) reload the page → the produced result image is still shown, (n) drag a block → edges follow smoothly with no jitter, (o) **fail a run then retry to success, reload → the success (image) is shown, not the stale failure**, (p) toolbar "Add result" → the block lands immediately right of its generation block, (q) generate music → the audio result block is wide enough to scrub the track comfortably, (r) drag a block, reload → the new layout persisted (move-only autosave), (s) set Length (seconds) on the music block → the generated track is that long; only ONE length field is visible in the Inspector.
