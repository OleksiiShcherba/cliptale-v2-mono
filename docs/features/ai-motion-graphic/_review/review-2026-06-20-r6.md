# Review — ai-motion-graphic — r6 (visual UI gate)

**Date:** 2026-06-20
**Type:** Independent visual end-to-end review (Playwright + real browser + real OpenAI API + real DB)
**Gate result:** **PASS**

## Method

Per the session goal, this round verified every user-facing flow from a **real client perspective** through the live UI — not a code diff read. Two **fully independent, clean-context agents** were dispatched; neither read the other's report nor any prior `_review/` artifact:

1. **Visual reviewer** (`visual-r1/`) — walked the §4 user stories / §5 ACs through Chromium (Playwright, headless, 1440×900), live OpenAI (gpt-4o), real DB.
2. **Independent decider** (`visual-decider/`) — repeated the walkthrough from scratch to render a fair final verdict, forbidden from reading any sibling report.

Environment: web-editor `:5173`, api `:3001` (`APP_DEV_AUTH_BYPASS=true`), OpenAI key live. The api container was recreated via `docker compose up -d --force-recreate api` so the real `APP_OPENAI_API_KEY` was loaded (it had been empty after a prior `docker restart`). Screenshots captured + visually inspected at every step.

## Scope

Whole feature diff on `feat/ai-motion-graphic` vs `master` (116 files). UI surfaces: `/motion-graphics` (list), `/motion-graphics/new` (author), `/motion-graphics/:id` (resume/refine), storyboard SceneModal attach.

## Per-AC result (both agents independently agreed)

| AC | User story | Verdict | Evidence |
|----|-----------|---------|----------|
| AC-01 | US-02 generate | PASS | duration set → cost confirm → real OpenAI authored a ready graphic with auto title + chosen duration |
| AC-02 | US-03 live preview | PASS | large preview fills canvas, chat alongside, duration input above; ≥2 time-separated frames differ → real-time animation |
| AC-03 | US-04 refine | PASS | chat refinement updated preview (e.g. gold/red title), exchange appended, version 1→2 |
| AC-04 | US-07 attach | PASS | attached to storyboard block; `motion_graphic_block_snapshots` row with frozen code + duration |
| AC-05 | US-02 invalid | PASS | too-short prompt declined inline with plain-language message (422, handled, no broken preview) |
| AC-11/AC-06 cost | US-06 | PASS | "Estimated cost USD 0.04" Cancel/Confirm gate; generation runs only after Confirm |
| AC-12 | US-08 duplicate | PASS | independent copy, chat seeded as live re-runnable turns, refinable without affecting original |
| AC-13 | US-01 list | PASS | list shows owner's graphics with title + duration + status (empty-state branch present in code) |
| AC-09 | determinism | PASS (indirect) | playback strictly from frame position; generated code uses `useCurrentFrame()` |
| resume | US-05 | PASS | reload `/motion-graphics/:id` restores full chat history + preview |

**Not exercised via UI** (negative/authorization/invariant paths with no organic UI trigger; backend-enforced + covered by codebase tests): AC-06 broken-generation, AC-07 cross-account 404, AC-08 attach-not-ready, AC-10 snapshot isolation, AC-14 broken-refine, AC-13 empty-state (account already owns graphics). No UI evidence of failure observed for any.

## Defects

**None blocking.** No forever-spinners, blank previews, error toasts, broken layout, or uncaught console/network errors during any MG flow. The only console line was the deliberate AC-05 422, surfaced as inline UI.

Pre-existing, **unrelated** observations (not this feature, not blocking): storyboard `generationDraft.service.ts:288` "blocks is not iterable" on the cards endpoint; one `404 /storyboards/:id/references/extraction`. Both outside ai-motion-graphic scope.

## Verdict

`VISUAL_REVIEW: PASS` (reviewer) + `DECIDER: PASS` (independent) → **gate PASS**. Every in-scope, UI-exercisable acceptance criterion visibly works end-to-end with the real OpenAI API and real persistence.

## Next

`/clear`, then `/sdd:ship ai-motion-graphic`.
