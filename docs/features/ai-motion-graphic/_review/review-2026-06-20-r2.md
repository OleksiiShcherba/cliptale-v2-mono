# Review — ai-motion-graphic (2026-06-20, independent re-run r2)

Mode: **fresh independent clean-context review + real-client visual review via Playwright with LIVE OpenAI** (real generate + refine + duplicate + attach, no mocks). This is a second, from-scratch pass that does **not** rely on the prior `review-2026-06-20.md` record — every AC was re-traced by a clean-context reviewer and every UI flow was re-driven against the real app + real OpenAI, with screenshots inspected directly.

## Scope

- Branch `feat/ai-motion-graphic` vs `master` (branch point `581c399`), **including** the uncommitted working-tree Anthropic→OpenAI migration (`lib/openai.ts`, deleted `lib/anthropic.ts`, config/cost/authoring/prompt edits, migration 062, new web cost/stream tests).
- Environment: `docker compose` up — api :3001 (`/health` ok), web-editor :5173 (200), db (healthy), redis. API container carries `APP_OPENAI_API_KEY` (set) + `APP_OPENAI_MODEL=gpt-4o`. The real OpenAI code-authoring path exercised end-to-end.

## What ran

### 1. Independent clean-context AC trace (`sdd:reviewer`, read-only, fresh artifacts)

Re-read spec §4/§5, sad §6, data-model, openapi, ADRs, test-plan, tasks.json itself (no paraphrase) and traced the WHOLE §4 US set (US-01..08) + §5 AC set (AC-01..14) end-to-end (spec → §6 sequence → data-model → api → tasks → code+test), across **both** `backend-service` and `web-frontend` target surfaces.

**Verdict: REVIEW_CLEAN** — every US and AC COVERED with a real code cite + a real asserting test. Security-sensitive set fully covered on backend AND UI:
- AC-07 existence-hiding 404 across all surfaces — `motionGraphic.service.ts:83` loadOwned→NotFoundError (open/refine/turns/duplicate/rename) + attach `storyboard.controller.ts:182`; tests CRUD `:247`, attach `:299`.
- AC-08 ready-gate on attach — `storyboard.controller.ts:185` (status≠ready→422) + picker pre-filter `:143`.
- AC-09 deterministic-render — `determinism.ts:87` AST scan + `:188` runtime shim + prompt `authoring.prompt.ts:36`; `determinism.test.ts`, `determinismE2E.test.tsx`, `frameDiffParity.test.tsx`.
- AC-10 snapshot isolation — snapshot is a COPY `storyboard.controller.ts:196`; `repository.test.ts:358` byte-identical after source mutate+soft-delete.
- AC-11 server cost re-validation — `cost.service.ts:113` exact-match; **seed 062 == client constant** `cost.ts:20`; `062-…migration.test.ts:67`.

**OpenAI migration:** complete and internally consistent — `lib/openai.ts` singleton, `config.ts:33-34` (`APP_OPENAI_API_KEY`/`APP_OPENAI_MODEL`), authoring `delta.content`/`finish_reason`/`config.openai.model`, cost `getPricingForModel(config.openai.model)`. No Anthropic/Claude references remain in runtime code; `@anthropic-ai/sdk` removed; docker-compose wired.

### 2. Visual flow — real UI walkthrough with live OpenAI (screenshots inspected directly)

- `mg-review.js` — list → nav entry → authoring → AC-05 short-gate → cost-confirm → **real OpenAI generate** → preview mount + **real-time playback** → persist route → **real OpenAI refine** → list-after → **duplicate** + seeded-live-chat open. Result: **14/14 pass**.
- `mg-attach-review.js` — storyboard → scene modal → Add Media → Motion Graphic → picker (18 ready) → attach (AC-04 snapshot) → reload persisted (200). Result: **6/6 pass**.

Screenshots inspected directly (not just asserted):

| AC / US | Flow | Visual evidence inspected |
|---|---|---|
| AC-01 / US-02 | Generate from prompt (live OpenAI) | `mg-05` — lower-third "Jane Doe / CEO" authored on dark bg |
| AC-02 / US-03 | Preview plays in real time | `mg-05` — player at `0:01 / 0:04`, full controls (pause/volume/scrubber/fullscreen), frame advancing |
| AC-03 / US-04 | Refine via chat (live OpenAI) | `mg-06` — "make text larger + fade in" → text visibly enlarged, fade-in bg, playing `0:02 / 0:04` |
| AC-05 | Too-short refusal | `mg-03` — inline red error "Add a longer, more detailed description…", generation not started, preview stays placeholder |
| AC-06 | Not-usable state surfaced | `mg-01` — FAILED status badges on graphics that failed |
| US-06 | Confirm cost before generating | `mg-04` — "Confirm generation … Estimated cost: USD 0.04" Cancel/Confirm |
| AC-12 / US-05/US-08 | Duplicate → independent copy w/ seeded live chat | `mg-09` — "Motion graphic (4s) (copy)" with 4 live turns (2 user + 2 assistant "Generated…"/"Updated…") |
| AC-04 / US-07 | Attach to storyboard block | `mg-13` — Edit Scene modal MOTION GRAPHICS section, snapshot player mounted (teal refined frame) |
| AC-13 / US-01 | Browse list + empty state | `mg-01` — title/duration/timestamp/status cards, "+ New motion graphic", Rename/Duplicate |

### 3. Test runs

- web-editor motion-graphic scope: **71/71 pass**. (Full web suite 3310/3313; the 3 failures are `storyboard/components/StepCorners.test.tsx` — untouched by this branch, pre-existing & out of scope.)
- API motion-graphic scope: service/cost/authoring/config **50/50**, guardrail unit+conformance **19/19**, DB-backed integration (CRUD/SSE/attach/repository/migrations 058-062) **74/74**. All green.

## Findings

Stage-1 (AC): **none** — all 14 AC genuinely satisfied; UI-visible ones confirmed by direct screenshot inspection, backend/security ones by code+test trace.

Stage-2 (quality), all non-blocking:

- **F1 — stale Anthropic-era doc comment in SSE integration test — FIXED.** `motion-graphic-sse-endpoints.test.ts:7-9` described the mock as yielding `content_block_delta` text-deltas (Anthropic shape) while the actual fake chunk is OpenAI `choices[].delta.content`/`finish_reason`. Comment corrected to the OpenAI chunk shape. Comment-only; no behavioral impact.
- **F2 — untracked root-level review scripts — DISMISSED (housekeeping).** `mg-review.js` / `mg-attach-review.js` at repo root are standalone manual Playwright review harnesses, not production code under `apps/`. They are review tooling; ship must not commit them as product artifacts. owner: Tech Lead, due: opportunistic.
- **F3 — 3 failing web tests in `StepCorners.test.tsx` — DISMISSED (out of scope).** Belong to the storyboard-pipeline feature; the files are untouched by this branch (confirmed vs base). Pre-existing, not introduced by ai-motion-graphic.

## Gate result

**PASS** — independent AC trace REVIEW_CLEAN (all 8 US + 14 AC traced end-to-end across backend + UI surfaces), full real-client visual flow confirmed with LIVE OpenAI (generate 14/14, attach 6/6, screenshots inspected directly), OpenAI migration complete & cost-gate seed aligned with the client constant and test-pinned. One trivial stage-2 comment fixed; two non-blocking items dismissed with reason. No open stage-1 finding.

Next: `/clear`, then `/sdd:ship ai-motion-graphic` (ship commits the Anthropic→OpenAI migration + migration 062 + the new cost/stream test files; do NOT commit the root-level `mg-*.js` review scripts).
</content>
</invoke>
