# Review — ai-motion-graphic (2026-06-20)

Mode: **independent clean-context review + visual review via Playwright with live OpenAI** (real generate + refine + attach), real-client perspective.

## Scope

- Branch `feat/ai-motion-graphic` vs `master` (branch point ~`581c399`), **including** the uncommitted working-tree Anthropic→OpenAI migration.
- Environment: docker compose up (api :3001 healthy, web-editor :5173, db, redis); `APP_OPENAI_API_KEY` set; the real OpenAI code-authoring path exercised end-to-end (no mocks).

## What ran

### 1. Independent clean-context AC trace (`sdd:reviewer`, read-only, fresh artifacts)

Traced the WHOLE §4 US set (US-01..US-08) + §5 AC set (AC-01..AC-14) end-to-end (spec → §6 sequence → data-model → api → tasks → code+test). **Verdict: CLEAN** — every US and AC COVERED with a real code cite + a real asserting test, including the security-sensitive ones: AC-07 existence-hiding 404 (`motionGraphic.service.ts:83` loadOwned → NotFoundError, CRUD test:247/286/347/376 + attach test:300), AC-08 ready-gate 422 (`storyboard.controller.ts:185`), AC-09 determinism scan (`determinism.ts:87` + `determinismE2E.test.tsx`, 16 tests), AC-10 snapshot byte-isolation under source mutation (attach test:241-251), AC-11 exact-match cost revalidation (`motionGraphic.cost.service.ts:113`).

**OpenAI migration:** complete and internally consistent — `lib/openai.ts` singleton, `config.ts` (`APP_OPENAI_API_KEY`/`APP_OPENAI_MODEL` default `gpt-4o`), authoring service (chat.completions stream, `delta.content`/`finish_reason`), prompt, cost service (`config.openai.model`), ADR-0002 revised, openapi updated, `@anthropic-ai/sdk` removed, `lib/anthropic.ts` deleted with no dangling imports. No leftover Anthropic references in any runtime code. All 39 API + 68 web unit/integration tests pass.

### 2. Visual flow — real UI walkthrough with live OpenAI

- `mg-review.js` — list → nav entry → authoring → AC-05 short-gate → AC-11 cost-confirm → **real OpenAI generate** → preview mount + **real-time playback** → persist route → **real OpenAI refine** → list-after → **duplicate** + seeded-live-chat open. Result: **14/14 pass**.
- `mg-attach-review.js` — storyboard → scene modal → Add Media → Motion Graphic chip → picker (16 ready) → attach (AC-04 snapshot) → persisted (history 200). Result: **6/6 pass**.

Screenshots inspected directly (not just asserted):

| US | Flow | Visual evidence inspected |
|---|---|---|
| US-01 | Browse list + nav entry | `nav-entry` PASS; `mg-01-list` / `mg-07-list-after` |
| US-02 | Generate from prompt (live OpenAI) | `mg-05-generate-result.png` — authored lower-third "Jane Doe / CEO" |
| US-03 | Preview plays in real time | `mg-05` — player at `0:01 / 0:04`, full controls bar (pause/volume/scrubber/fullscreen), frame advancing |
| US-04 | Refine via chat (live OpenAI) | `mg-06-refine-result.png` — "make text larger + fade in" → text visibly enlarged, playing `0:02 / 0:04` |
| US-05 | Resume / hydrate chat | `mg-09-duplicate-open.png` — seeded live turns (2 user + 2 assistant: "Generated…"/"Updated the motion graphic.") |
| US-06 | Confirm cost before generating | `mg-04-cost-modal`; `Est. USD 0.04` shown above chat |
| US-07 | Attach to a storyboard block | `mg-13-attached.png` — Edit Scene modal MEDIA → MOTION GRAPHICS, snapshot player mounted |
| US-08 | Duplicate → independent copy | `mg-08` / `mg-09` — "Motion graphic (4s) (copy)" with seeded live chat |

Backing tests: 68 web-editor MG unit tests + 39 API tests green; attach controller 5/5; picker 4/4.

**Fixture note:** the visual attach harness needs a seeded storyboard draft (`aaaaaaaa-…-0001`, owner `dev-user-001`) with a scene block; the prior run's draft had been reaped from the dev DB, producing a `GET /storyboards/…001 404`. Re-seeded the draft + one scene block (sentinels auto-insert on load) — not a product defect; the attach code is unchanged and passed once the fixture existed.

## Findings

Stage-1 (AC): **none** — all 14 AC genuinely satisfied and visually confirmed.

Stage-2 (quality / hardening), all non-blocking:

- **F2 — load-bearing pricing seed (migration 062) was untested — FIXED.** Without `flow_model_pricing.per_second = 0.010000` the server estimate falls to `0.0000` while the client sends `0.01×duration`, rejecting 100% of generations (422). Added `apps/api/src/db/__tests__/062-motion-graphic-authoring-pricing.migration.test.ts` (4 tests, green) pinning the seeded rate.
- **F3 — client/server cost constant coupling was unpinned — FIXED.** Added `apps/web-editor/src/features/motion-graphic/cost.test.ts` (3 tests, green) pinning `MOTION_GRAPHIC_COST_PER_SECOND === 0.01`, the formula, and the 0-duration edge — cross-referenced to migration 062.
- **F4 — sidebar/`/new` nav-entry has no unit test — DEFERRED (non-blocking).** The entry point is already exercised and visually confirmed by the live flow (`nav-entry` check PASS); a dedicated unit test is informational hardening. owner: Tech Lead, due: opportunistic.

## Gate result

**PASS** — independent AC trace CLEAN (all 8 US + 14 AC traced end-to-end), full visual flow confirmed from a real-client UI walkthrough with live OpenAI (generate 14/14, attach 6/6, screenshots inspected directly), and the two highest-value stage-2 hardening guards added (both green, test-only). One informational nav-test deferred. No open stage-1 finding.

Next: `/clear`, then `/sdd:ship ai-motion-graphic` (ship commits the uncommitted Anthropic→OpenAI migration + these two new test files).
